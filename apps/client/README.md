# 万宝单生活客户端

2026-09-19 当前基线说明：本工程包含 V3 多平台首页结构、首页 / 领券 / 推广 / 我的四栏导航、推广身份状态组件与商品链接准备页，客户端显示名称已按 DOC-08 更新为「万宝单生活」。M1-01c 验收结构与本地导航；M1-01d 提供券目录、地域与详情只读接口接入及上下文失效保护，功能验证使用模拟传输与本机 HTTP 测试服务，不代表已取得真实渠道数据。商品 / 店铺、真实授权、领取 / 购买及业务闭环仍未完成，不能把新版设计或京东签名客户端合入视为真实业务已接通。AppID / 包名 / 渠道媒体 ID 和历史分享文案不因展示名称更新而更改。

uni-app / Vue 3 / TypeScript 独立客户端；H5 首发。使用受支持的 Node.js LTS：20.19+ 或 22.12+。

```sh
cd apps/client
npm ci
npm run dev:h5
```

开发入口 `http://127.0.0.1:5173`。开发服务器只绑定本机，关闭跨域响应和 HMR，文件访问限制在客户端目录；修改代码后手动刷新。券只读路径通过开发代理连接 `http://127.0.0.1:8080`，不代理推广身份、领取等路径；未启动已正确配置的 Go API 时显示读取失败，不内置成功数据。不要暴露到公网，仅绑定本机不能消除旧 Vite 的全部风险。

```sh
npm test
npm run typecheck
npm run build:h5
```

H5 产物在 `dist/build/h5`。node_modules、产物、环境文件不进入 Git。包版本遵守 uni-app 官方 Vue 3 稳定模板的同版约束；构建链安全审计需在发布前独立完成，不使用 `--force` / `--legacy-peer-deps` 掩盖版本冲突。

2026-09-17 锁文件审计：40 项（15 low / 12 moderate / 13 high / 0 critical）；独立 Vitest 已升级 4.1.11。uni-app 插件仍精确要求 Vite 5.2.8，含跨站读取开发服务器等风险；当前防护不是所有漏洞的全面修复。安全升级、兼容回归与发布安全验收未通过前，不允许生产发布；不得将上述工程检查称为安全审计通过。

## 当前边界

REL-01a-3a 扩展 `test:layout` 到真实本人订单 / 看板页面，QA-only 提供方身份与 uni HTTP 拦截；320 / 390 / 1280px 的 200% 文本、128 字符推广位、渠道 / 日期筛选、归因详情 / 错误重试 / 零态 / 401 清理。订单和统计输入外框由 50px 改 60px，原生可用高度由 24px 改 34px，避免 28px 放大文字裁切。日期仍分别 UTC 时刻范围 / 上海含首尾自然日，不改口径。可选 `CLIENT_QA_SCREENSHOT_DIR` 指向已有外部目录保存本机合成数据截图；未设置不写截图。非空券 / 适用商品的放大验收由 REL-01a-3b 继续，测试身份不代表真实 JWT / 渠道或辅助技术验收。

REL-01a-2 首页标题区支持内容驱动换行，搜索区域保留布局宽度且输入至少 44px 高，避免 200% 大字号下宽度被挤至 19px 或文字被 18px 原生高度裁切。真实布局回归使用 `npm run test:layout`（Node / Playwright，不走 jsdom）；需先启动 `npm run dev:h5`，并提供已有 Playwright / Chrome。模块可由 `CLIENT_QA_PLAYWRIGHT_MODULE` 指定现有模块路径，Chrome 由 `CLIENT_QA_CHROME_EXECUTABLE` 指定，目标由 `CLIENT_QA_URL` 指定（默认 `http://127.0.0.1:5173`）；未提供且本机无依赖时测试明确失败，不自动安装或跳过。例：环境已配置上述路径后执行 `npm run test:layout`。关联与验收范围见 [七页面验收矩阵](../../docs/28-client-accessibility-acceptance-20260919.md)；200% 用户样式文本不是浏览器缩放、系统字体或完整辅助技术验收。

REL-01a-1 修正链接准备页快速输入后立即清空：重建文本域以失效 uni H5 尚未同步的原生编辑与节流事件，清空后不会恢复旧文本。H5 在组件根范围监听原生节点替换并重新设置无障碍名称，退出时解除监听；不观察全局文档或属性变更。原剪贴板 pending 防护与识别未开放状态保留。实际 Chrome 320 / 390 / 1280px 快速两次输入 → 键盘清空 → network idle 仍为空且新原生节点名称正确；不是微信真机或辅助技术全量验收。

M3-04b-2a 提供 `features/promotion/dashboard-model.ts`，在 Vue 页面 setup / effectScope 创建或由独立所有者负责 `dispose()`，默认无身份无请求，初次读取显式调用 `refresh()`，换会话同步清理筛选 / 计数 / 错误后用新提供方令牌重读。刷新与筛选立即删除旧快照，版本保护忽略迟到结果；同会话任意迟到 401 清空并禁止复用当前令牌，旧身份失败不影响新身份。真实三个零计数保留服务端时间元数据并标记空态，不把错误、未接入或缺数据变成零。复制多于链接 / 订单也不推算漏斗。相同筛选不自动重复读取，手动刷新用于重试；非法对象 / 未批准字段或日期失败锁闭，不转成查询全部。作用域销毁自动停止监听、清缓存与失效请求，销毁后会话变化 / 刷新 / 筛选 / 迟到响应均不恢复数据。模型验证使用真实读取契约加模拟传输；页面 / 键盘 / 窄屏浏览器由 M3-04b-2b 继续，真实数据对账及身份由 M3-04b-3 验收。

M3-04b-1 提供 `features/promotion/dashboard-api.ts` 本人看板 GET 契约，只有成功转链 / 复制上报 / 有效归因订单三项服务端计数，没有金额、收益或转化率。筛选 `from` / `to` 为上海 YYYY-MM-DD 自然日、都包含当天（不同于订单页的 UTC RFC3339 筛选），支持渠道 / 推广位；默认范围由服务端决定，验证为 30 个自然日，不用客户端时钟构造。校验 Asia/Shanghai 真实日界 / 历史夏令时、数据库 `asOf`、边界匹配、后端实际时长上限 366×24 小时与非负安全整数计数；即使复制大于链接 / 订单数也不推导漏斗或交易。时间戳保留服务端值，后端新规范输出 UTC，日历仍为上海；运行时缺少真实上海 Intl 时区支持时失败锁闭，不退回固定 UTC+8 或模拟计数。

看板身份提供方须显式传入 getter，客户端不签发 / 保存令牌；仅提供方 Bearer 请求头与 10 秒 uni GET，令牌不放 URL。缺失 / 退出 / 换会话拒绝响应，筛选复制为请求快照，输出仅批准计数与时间字段，错误不暴露内部消息。65 项模拟传输测试及编译只证明读取契约；真实身份、获批数据、可信点击、收益快照、微信设备时区能力与业务对账尚未验收，开发代理仍不转发推广 API。

M3-04b-2a 已实现看板状态模型；M3-04b-2b-1 的 `PromoterDashboard.vue` 实现三项只读计数、上海自然日筛选与服务端精确快照、真实零 / 错误恢复、未登录隐藏及换身份清理。日期按 IANA 当地日历转换，不固定 UTC+8，不以实际时刻减 24 小时计算结束日期。14 项组件测试使用真实模型 / API，仅模拟外部传输；包含 DST / 历史 offset 秒、输入 blur 和初始筛选同步。

M3-04b-2b-2 接入独立 `/pages/promotion/dashboard`，推广中心入口和返回支持键盘，页面 setup 创建模型并自动销毁。默认没有提供方身份时不请求；生产代码没有 QA 身份或测试计数，不提供 token 输入。4 项页面测试及 320 / 390 / 1280px 实际 Chrome 浏览器验收通过：QA-only 提供身份与拦截真实 uni HTTP，验证筛选、错误恢复、零计数、节流输入重置、换身份 / 退出 / 401 清理。项目 334 项测试、类型检查、双端构建通过；仅本机模拟业务，不代表真实身份、生产 API 配置、微信真机、真实渠道计数或资金验收。模型浏览器 / 页面范围已完成，真实业务 M3-04b-3 仍待依赖。

M3-02c-2b 新增独立 `/pages/promotion/orders`，从推广中心进入；消费者 `/pages/orders/index` 保持原职责。展示脱敏列表、渠道 / 订单状态 / 推广位 / UTC 日期筛选、分页与重试、归因详情 / 已应用状态历史 / 退款事件数，明确订单状态不代表收益或到账。日期格式 YYYY-MM-DD，开始与结束当天包含在内，结束日期转换为次日 UTC 的排他 `to`。输入框外层至少 50px；失焦提交 uni H5 节流前的最终值，清除筛选销毁旧输入实例，避免迟到输入恢复旧草稿。

真实身份所有者可通过 `promoterSessionKey` 提供身份 Ref；订单页面只读这个提供方，默认 `null`、不请求、不提供填写 token 的表单，不签发 / 持久化模拟令牌。模型在页面 setup 中创建，生命周期由页面作用域清理。筛选重置与直接换会话也销毁旧输入实例，不让原账户的迟到输入恢复到新会话。模拟传输组件测试及浏览器 QA 只在测试端提供身份与拦截 HTTP，生产代码没有测试订单或假会话。当前开发代理仍不转发推广认证 API，未接真实身份和生产 API 配置；页面基础验证不等于真实登录、订单归因或渠道 / 金额业务验收。

订单模型生命周期：模型在 Vue 页面 setup / effectScope 内创建时，作用域结束会自动停止会话监听、清空缓存并失效待处理请求；独立创建的所有者应在视图弃用时调用幂等 `dispose()`。销毁后刷新、筛选、分页与详情动作不再发送请求，不随身份 Ref 变化恢复缓存。应用级共享实例也须在其所有者退出时销毁；不依赖测试专用清理机制。

M3-02c-2a 提供 `src/features/promotion/orders-model.ts`，复用已验证读取契约；显式传入身份层 Ref，首次读取由页面调用刷新，会话变化同步清除旧筛选 / 订单 / 详情 / 游标，新会话重新读取，无会话不请求。列表与详情分别忽略旧响应，分页失败保留游标、拒绝重复页与游标循环。同一会话的任意 401（包括旧筛选请求）清除全部缓存并禁止复用该令牌；旧会话结果不影响新会话，不修改身份提供方。详情新状态更新摘要，状态 / 日期不再匹配时隐藏旧列表项并保留详情，禁止旧目录页恢复。模型须在所属 Vue 页面 setup 中创建，使会话监听随页面生命周期清理；若显式作为应用级共享实例创建，则应只保留一个应用级实例。该任务原验收仅状态与模拟传输，后续页面与窄屏基础验证见 M3-02c-2b；真实会话验收仍待身份接入。

M3-02c-1 提供 `src/features/promotion/orders-api.ts`：仅本人推广订单列表 / 详情 GET 契约，非消费者返现订单，响应没有金额 / 收益。会话提供方必须显式注入，客户端不签发、不持久化假令牌；无可用令牌不请求，默认 uni 仅使用提供方令牌的 Authorization Bearer、10 秒超时，令牌不放 URL，等待期间令牌改变或退出时拒绝旧响应。筛选绑定请求快照；校验随机公开 UUID、脱敏编号、状态 / 渠道 / 推广位 / 归因方法 / RFC3339 日期、分页及历史，返回仅批准字段，额外买家 / 原始证据 / 收益字段不进入结果。该任务原验收只有模拟传输与编译，后续页面见 M3-02c-2b；开发身份代理与真实会话仍未接入，不代表真实订单 / 认证 / 金额验收。

已实现：首页京东 / 美团 / 淘宝 / 拼多多 / 饿了么平台选择，推广商品 / 优选商店 / 领券中心 / 推广专区四模块及四栏导航；首页与领券页共享当前平台、地域 / 业务范围和只读券数据，平台改变清除地域 / 业务选择与旧提示，但不表示渠道已获授权。「我的」保留订单 / AI / 推广入口，订单与 AI 通过二级路由访问；分类 / 省钱旧路由保留，不再占底栏。H5 支持入口、平台选择、地域 / 业务操作和底栏 Enter / Space。商品、店铺、订单、AI 与推广业务仍显示未接入说明；券页区分读取中、无数据、读取错误、服务未接入和平台未开放。微信端虽可编译，SVG 导航图标与实际设备行为仍须单独适配验收。

工程阶段不内置演示订单、商品、返现、收益或成功转链。真实身份提供方、协议内容、获批渠道和资金规则未提供时，对应功能明确显示未接入，不可提交真实业务。

## 只读券接口契约

REL-01a-4 新增 `tests/browser/link-input-text-resize.browser.cjs`，真实准备页 / LinkInput，测试浏览器只替换 uni 剪贴板边界，不访问系统剪贴板。三宽度 / 200% CSS 文本验证长文案、读取失败 / 空 / 超限保留原输入、等待读取禁用 / 恢复、动态原生输入名称、键盘清空与识别关闭；无 API 请求。`test:layout` 当前共 6 项，不等于微信权限 / 真实系统剪贴板验收。

REL-01a-3b-1 修复业务代码快速修改后应用旧值：按钮应用通过 uni blur 提交最新值，输入 Enter 使用包含最新值的 uni confirm，不提前在 keydown 应用节流旧值；上下文改变重建输入，避免旧输入回填。组件 blur / confirm 回归及 `tests/browser/coupon-business.browser.cjs` 三宽度快速输入 / 平台重置回归归入 `test:layout`。仅本机 HTTP 测试数据，不代表渠道或微信真机验收。

REL-01a-3b-2 的 `tests/browser/coupon-text-resize.browser.cjs` 使用实际页面 / uni GET、测试端 HTTP 合成券与商品，验证三宽度 / 200% CSS 文本、长字段、城市 / 业务键盘筛选、分页 503 保留和显式重试、商品错误 / 空 / 恢复、关闭与未开放平台清理、只读提示与错误脱敏。该任务完成时 `test:layout` 共 5 项；运行前提与外部可选截图目录沿用上述说明，不安装浏览器依赖或新增生产模拟数据。不表示全部字段 / 系统字体 / 微信真机或真实渠道领取验收。

M6-02b-3a 扩展适用商品 `/api/v1/coupons/{id}/products` 只读分页契约：券 ID、地域 / 业务与游标编码，固定每页 20；校验完整商品 ID / 标题 / 更新与有效期、重复 ID、分页结构和过期数据。接口不含实时价格或购买地址，不因商品名称而开放领取 / 下单。

M6-02b-3b 在已验证平台的券规则详情下读取适用商品，活动券不请求商品列表；商品响应自身不含平台，不能单靠商品 ID 判断归属。支持加载 / 空 / 错误、刷新与分页重试，关闭或切换详情 / 平台 / 地域 / 业务时清除商品与游标并忽略旧响应。到期商品隐藏，券到期或商品请求返回券已失效时，清空商品并失效规则；分页仍有下一页时可继续。开发代理只扩展具体券的 `/products` 只读路径，不代理领取、任意子路径或推广身份。仅本地测试数据与编译验收，尚未取得真实渠道商品或购买能力。

M1-01d-1 提供 `src/features/coupons/api.ts`：目录 `/api/v1/coupons`、地域 `/api/v1/coupon-cities`、详情 `/api/v1/coupons/{id}`，仅 GET，uni-app 请求超时 10 秒，无内置令牌或授权头。平台映射 JD / TAOBAO / MEITUAN → JD / TB / MT；PDD / ELEME 保持未开放，不发请求。只接受成功信封、完整字段、当前平台与地域 / 业务范围、非过期材料及一致的领取模式标签；错误不展示服务器内部信息。

M1-01d-2 将契约连接首页摘要（最多两条）与领券页；地域选项只使用服务器结果，未选城市时只显示不限城市活动，选城市后仍包含不限城市活动；业务留空只显示不限业务活动。切换平台 / 地域 / 业务先清空列表、详情与游标，旧响应不能回填。地域初次加载失败时仍可读取不限城市活动；已有明确城市选择不因暂时地域错误擅自改变。详情只显示规则和更新 / 有效期，目录与详情可显式重试；更多页失败保留同一游标重试，到期数据会隐藏并失效详情。

不提供领取 / 购买能力，不使用商品示例或虚构券作为生产兜底。H5 生产产物使用同源 `/api/v1/...` 路径，需要部署方正确配置 API 路由；本轮没有生产部署或现有数据库迁移。跨端编译不表示相对 API 路径、微信合法域名或实际设备已验收；真实渠道及数据验收仍独立进行。

`npm run build:mp-weixin` 仅供后续编译检查，不能代替真实微信 AppID、合法域名配置、开发者工具和设备验收；manifest 中不填假 AppID。

规范与任务见 `docs/superpowers/specs/2026-09-17-wanhui-client-design.md`、`docs/superpowers/plans/2026-09-17-wanhui-client-foundation.md`。
