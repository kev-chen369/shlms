# 部署设计

`DEV`、`TEST`、`STAGING`、`PROD` 完全隔离，分别使用独立数据库、缓存、消息队列、对象存储和渠道凭据。V1 可使用容器编排，流量与团队规模不足时不强制 Kubernetes。

发布流：构建不可变镜像 → 兼容性迁移 → 预发布验证 → 小流量灰度 → 指标观察 → 全量。迁移采用先扩展后收缩，发布期间新旧版本可并存。

凭据由密钥管理系统注入，日志脱敏，服务间采用最小网络权限；备份需定期恢复演练。

保留上一镜像和配置版本。不可逆数据迁移先备份并提供前向修复方案，不能把删除性迁移当作普通回滚。
