# 监控与告警

技术指标包括 API 可用率与 P95/P99、错误率、DB/Redis/MQ、任务延迟、队列积压、渠道成功率和熔断状态。

业务指标包括访问 → 浏览 → 推广点击 → 跳转 → 下单 → 有效订单 → 佣金 → 结算漏斗，以及 GMV、退款率、漏单率、毛利、提现失败和对账差异。

高优先级告警包括订单同步停滞、钱包不平、重复入账、提现异常、密钥失效和结算差异突增。告警设置持续时间避免抖动，并附运行手册与负责人。

使用统一 `request_id`、`tracking_id`、内部订单号和渠道订单号串联日志、指标与追踪。敏感字段只保留掩码。
