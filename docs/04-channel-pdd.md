# 拼多多/多多进宝渠道适配设计

通过实际获批的 DDK 能力完成商品检索、推广链接和订单同步。内部记录应用、PID、推广位及渠道支持的自定义归因参数，禁止在日志记录密钥。

适配层把渠道商品标识、价格、券、佣金率和有效期转换为统一模型；金额单位只转换一次，并执行范围与币种校验。

订单先原样持久化，再映射内部状态。定时任务使用重叠窗口、分页游标和幂等键；退款、审核失败和佣金变化使用差额流水，不覆盖历史资金记录。

验收覆盖商品、券、转链、归因、已付、确认、退款、无效和结算，并核对控制台报表与内部对账。
