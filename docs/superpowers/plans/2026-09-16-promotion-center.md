# 推广中心研发计划

日期：2026-09-16。依据：[详细设计](../../21-promotion-center-detailed-design.md)、[模型与图册](../../20-promoter-model-user-ui-design.md)、[既有基础计划](./2026-09-15-mvp-foundation.md)。本计划新增推广模块任务，不代表已完成开发；未排具体工期，待人员、前端技术栈及渠道能力确认后估算。

## 执行规则

逐任务遵循仓库 [AGENTS.md](../../../AGENTS.md)：每项带编号与明确状态，完成一项、验证一项、更新一次记录并立即提交一次 Git。`[ ]` 搭配未开始 / 进行中 / 阻塞，只有完成验收才使用 `[x]【已完成】`。新增或拆分任务分配新编号，不重排已有编号。提交信息包含任务编号，以 Git 历史追踪提交；本地 commit 不等于已推送远端。

完成记录格式：`任务编号｜完成日期｜变更范围｜验证命令及结果｜遗留限制`。进行中 / 阻塞记录需注明当前进展、原因和下一步。

## 分支整合

- [x] PROC-08【已完成】2026-09-19：同步已验收推广订单页面及看板接口 / 模型、文档与实际页面截图。独立工作树隔离未完成的看板组件 / 测试；复验后归档，fetch / merge 最新 main，非强制推送并核对。没有生产部署或数据库迁移。
  - [x] PROC-08a【已完成】2026-09-19：独立工作树归档订单未登录 / 列表 / 详情三张 390px 实际截图，更新最新同步说明与文档 / 图片索引。客户端 316 项测试、类型检查、H5 / 微信构建，Go 全量测试 / vet / build，原生 H5 13 项测试通过；Playwright / Chrome 320 / 390 / 1280px 复验通过，39 次只读请求、9 条预期 HTTP 错误、无其他运行错误；新增图片 PNG 签名 / 非零尺寸与相关 Markdown 链接、diff 检查通过。PG_TEST_DSN 未配置，数据库集成跳过；仅本机测试数据，不代表真实业务或生产部署。未完成看板组件及测试不纳入发布。
  - [x] PROC-08b【已完成】2026-09-19：fetch / merge origin/main 815172d（Already up to date），HTTPS 推送因本机无用户名认证失败，使用既有 SSH 身份非强制推送 main 至 ce8e516；重新 fetch，以 `git rev-parse HEAD origin/main` 确认双方一致。已同步四项功能及 PROC-08a 文档 / 三张截图，保留远端历史，无生产部署或数据库迁移。本核对记录独立提交并推送，不将自身哈希循环写入记录。

- [x] PROC-07【已完成】2026-09-19：按用户要求同步本地四项已验收功能、相关文档与适用商品页面截图到 GitHub main；fetch / merge 最新 main 无冲突，复核测试与截图后非强制推送并核对远端一致。不部署生产或迁移数据库。
  - [x] PROC-07a【已完成】2026-09-19：fetch / merge origin/main 8e4edaa（Already up to date），复核 M6-02b-3a / M6-02b-3b / M3-02c-1 / M3-02c-2a；归档 320 / 390px 适用商品区域截图，更新同步说明、文档与图索引。客户端 217 项测试、类型检查、H5 / 微信构建；`go test -count=1 ./...`、`go vet ./...`、`go build ./...`、原生 H5 Node 13 项测试及 diff 检查通过。已有 Playwright / Chrome 在 320 / 390 / 1280px 验证真实 uni 请求的本机 HTTP 测试数据、分页 / 重试 / 键盘 / 到期 / 失效 / 旧响应；51 次无授权 GET，9 条预期 404 / 503 日志，无其他运行错误；直接检查两张截图及 PNG 签名 / 非零尺寸和路径。PG_TEST_DSN 未设置，数据库集成跳过；订单页面未实现，不代表真实身份、订单、渠道或资金验收。独立提交，不部署或迁移。
  - [x] PROC-07b【已完成】2026-09-19：再次 fetch / merge origin/main（Already up to date），通过 SSH 非强制推送 GitHub main 8e4edaa→e9a3590；重新 fetch 并以 `git rev-parse HEAD origin/main` 确认双方均为 e9a3590。四项功能提交及 PROC-07a 文档 / 图片均已同步，保留远端历史。当前核对记录独立提交并推送，不将本次提交哈希写入自身。仅仓库同步，没有生产部署、数据库迁移或真实渠道业务验收。

- [x] PROC-06【已完成】2026-09-19：按用户要求更新 GitHub main 代码、文档与图；纳入 M1-01c、M1-01d-1、M1-01d-2 已验收提交，合并 origin/main 并归档实际页面截图、更新图册索引及复核合并结果。非强制推送后重新 fetch 核对双方一致；不部署生产、不迁移数据库、不把截图测试券当真实渠道数据。
  - [x] PROC-06a【已完成】2026-09-19：合并 origin/main 58b4549，无冲突；归档两张 390px 实际页面截图、更新图片索引和同步说明。合并后客户端 109 项测试、类型检查、H5 / 微信构建、`go test -count=1 ./...`、`go vet ./...`、`go build ./...`、`node --test web/*.test.mjs` 13 项与 diff 检查通过；39 张 PNG 签名 / 非零尺寸及新增截图链接存在性检查通过。未设置 PG_TEST_DSN，数据库集成跳过；浏览器本机 HTTP 测试数据，不代表真实业务。远端推送单独由 PROC-06b 核对。
  - [x] PROC-06b【已完成】2026-09-19：再次 fetch / merge origin/main（Already up to date），经 SSH 非强制推送 GitHub main 58b4549→60f7cb6；重新 `git fetch origin` 及 `git rev-parse HEAD origin/main` 确认双方均为 60f7cb6，完整保留远端历史。当前记录单独提交，不将本次提交哈希循环写入自身；无生产部署或数据库迁移。

- [x] PROC-05【已完成】2026-09-19：按用户要求同步 GitHub main 的已验收代码、当前文档及既有图稿；保留原工作区未验收的 M1-01c 改动。不包含生产部署、数据库迁移、真实渠道验收或重新绘图。
  - [x] PROC-05a【已完成】2026-09-19：在独立工作树复核远端 71da903 与已验收品牌提交 M0-02f，修正图片索引的 V3 已确认结构和历史名称说明；未改变既有 PNG。验证：`go test ./...`、`go vet ./...`、`go build ./...`、`node --test web/*.test.mjs`（13 项）、客户端 `npm test`（43 项）、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin` 全部通过；Node 检查 37 张 PNG 签名 / 非零尺寸及索引文件路径通过，`git diff --check` 通过。未配置 PG_TEST_DSN，数据库集成测试跳过，不代表真实渠道、生产数据库或微信真机验收；远端推送另由 PROC-05b 核对。
  - [x] PROC-05b【已完成】2026-09-19：重新 fetch / merge origin/main（Already up to date），通过 SSH 非强制推送已验收品牌代码与图稿索引记录；再次 `git fetch origin`、`git rev-parse HEAD origin/main` 核对双方均为 4025e18，完整保留远端既有历史。仅 GitHub 仓库同步，未执行生产部署或数据库迁移。`npm ci --ignore-scripts` 完成，审计仍为 40 项（15 low / 12 moderate / 13 high），继续由 M0-02e 跟踪，不代表安全发布门槛通过。

- [x] M0-02f【已完成】2026-09-19：按 DOC-08 已确认名称统一 uni-app 首页、H5 HTML / manifest 标题、跨端 manifest 显示名称及全局导航标题为「万宝单生活」，更新当前 README；保留 AppID / 包名 / 渠道媒体 ID、版本号、公开分享文案与 V2 历史验证记录。首页品牌测试先实际失败，实现后 `npm test`（43 项）、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin` 通过。Browser 插件未提供，使用现有 Playwright / Chrome 在本机 127.0.0.1:5173 验证 320×844、390×844、1280×844 的实际标题、首页新名称、无旧名称 / 框架覆盖 / 运行错误 / 横向溢出，首页品牌与城市按钮不重叠、五栏保留且推广入口到达；直接查看 320px 截图。只读代码审查无阻断，`git diff --check` 通过。仅客户端品牌适配，不代表 V3 页面、微信设备、图稿重绘或真实业务验收；本地逐任务提交，不自动推送 / 部署。

- [ ] PROC-04【进行中】整合远端 `main` 的 uni-app H5 客户端与后端安全修复，以及本分支 V3 首页、领券、订单、看板和推广流程；统一迁移、分享事件契约与设计基线。远端客户端的 V2 导航仍需按已确认的 V3 四栏方案改造，不能把两个入口同时视为最终产品。
  - [x] PROC-04a【已完成】2026-09-18：合并远端 uni-app 客户端及后端事务安全修复，以远端 000010 分享事件迁移为共同基线，将本分支券与订单迁移顺延为 000011～000021；保留两个已发布分享 HTTP 路径，映射到同一张按用户 / 事件号去重的表，并补跨契约同键重放 / 冲突测试。更新数据库说明和原生 H5 在无 `navigator` 环境下的默认剪贴板访问。验证：Go 1.26 + 隔离 PostgreSQL 16 `go test -count=1 ./...`、`go vet ./...`；uni-app `npm test` 43 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`；原生 H5 Node 13 项、`git diff --check` 均通过。限制：仅证明全新隔离库迁移和模拟接口，旧功能分支数据库的保留数据升级待 PROC-04b；uni-app 仍为 V2 页面，V3 设计与真实渠道验收未完成。
  - [ ] PROC-04b【未开始】确认此前功能分支迁移 000010～000021 是否曾用于需保留数据的数据库；如有，编写并演练受控迁移与回滚，不直接改写已应用迁移账本。已向用户询问部署状态，收到信息前不对现有数据库执行整合迁移。

## 当前基线

- [x] DOC-01【已完成】首页推广入口方案与图稿纳入详细设计（P-00）。
- [x] DOC-02【已完成】P-01～P-16 核心图稿、接口目标、数据约束和验收要求归档。
- [x] DOC-03【已完成】2026-09-17：补绘多平台切换 H5 首页 V3 概念稿，展示京东选中态、推广商品 / 优选商店 / 领券中心 / 推广专区及首页 / 领券 / 推广 / 我的四栏；更新前端规格与图片索引，明确演示数据及与 V2 五栏方案的差异。验证：人工核对 PNG 的平台、四模块与四栏标签；图片可读取、相对链接存在、`git diff --check` 通过。限制：仅视觉概念稿，尚待产品评审；没有 H5 工程、真实渠道数据或业务验收，不勾选 M1-01 / M0-02。
- [x] DOC-04【已完成】2026-09-17：按用户确认将 V3 多平台首页定为 P-00 / M1-01 当前设计基线，修订前端规格、推广详细设计与模型图册，将 V2 五栏和双卡标为历史方案。验证：相关图稿与历史说明链接均存在，任务状态核对通过，`git diff --check` 通过。限制：仅设计决策及任务调整，H5 尚未开发，M0-02 / M1-01 和真实渠道验收仍未完成。
- [ ] DOC-05【进行中】将现有 21 组 V2 图稿统一为 V3 视觉体系。先盘点图稿与页面、制定统一色彩 / 字体 / 卡片 / 四栏导航规则，按下列可独立验收的子任务逐组替换；原 V2 文件保留作历史归档，V3 文件另存。当前正在设计领券、推广一级页面，其余分组仍未开始。
- [x] DOC-06【已完成】2026-09-17：新增领券到下单设计，明确单商品 / 品类 / 店铺 / 外部券去向、领取回执、未知状态、商品复核、购物 Tracking 与真实订单边界；同步前端、业务流程和 API 目标说明，并拆分 M6 研发任务。验证：文档相对链接存在，设计覆盖成功 / 失败 / 超时 / 越权验收，`git diff --check` 通过。限制：仅设计与任务调整，没有 H5、真实领券接口、购物转链或订单验收。
- [x] DOC-07【已完成】2026-09-17：新增 V3 推广选品图稿和详细设计，按京东 / 淘宝 / 美团、商品 / 活动逐物料设计预览、选择推广位、生成中、查询、复制及记录；核对官方资料和现有京东代码边界，新增 P-17 与 M7 分平台研发任务。验证：图稿人工核对平台 / 物料 / 禁用状态，PNG 与文档相对链接可读，`git diff --check` 通过。限制：仅设计；京东真实单品、京东活动、淘宝、美团均无生产端到端成功证据，未修改渠道 readiness。
- [x] DOC-09【已完成】2026-09-17：将京东、淘宝联盟、美团券的本站入口、App / H5 / 小程序承接、平台领取 / 结算 / 到店核销和可验证状态补入领券到下单设计。验证：核对官方渠道资料、券类型与状态表，`git diff --check` 通过。限制：仅设计分析；无真实渠道权限、领券回执、订单或核销验收。
- [x] DOC-10【已完成】2026-09-17：依据 V3 领券界面与 DOC-09 分析，更新领券到下单、前端、业务流程、API 目标契约及 M6-01～M6-08 任务和验收边界。验证：逐项核对券模式、按钮、跳转 / 回执状态及任务编号，`git diff --check` 通过。限制：仅设计与研发计划更新；无 H5 实现或真实渠道验收。
- [x] DOC-08【已完成】2026-09-17：用户将 APP 对外名称确定为「万宝单生活」；同步产品总设计、文档索引、前端规格和京东接入说明，区分显示名称与真实包名、备案域名及获批媒体 ID。验证：核对四处名称说明与 DOC-08 链接、`git diff --check` 通过。限制：历史 V1/V2 图稿保留待 V3 重绘，尚未创建京东应用或取得媒体 ID；仅文档更新，无真实业务验收。
  - [x] DOC-05a【已完成】2026-09-17：设计与 V3 首页一致的 H5「领券」一级页，包含平台筛选、精选 / 品类 / 即将过期券及四栏底导航；移除初稿中过期的日期文案，保留演示数据提示。验证：人工核对页面模块、领券选中态、四栏标签、日期文案与 PNG 可读，相关文档链接存在，`git diff --check` 通过。限制：仅图稿，无真实券数据和领取流程验收。
  - [x] DOC-05b【已完成】2026-09-17：设计与 V3 首页一致的 H5「推广」一级页，示例为身份已启用、京东渠道位待配置；显示推广位、禁用转链、订单与收益空态及四栏底导航。验证：人工核对身份 / 渠道状态、禁用按钮、四栏标签与 PNG 可读，相关文档链接存在，`git diff --check` 通过。限制：仅图稿，未实现推广 H5、真实渠道位和订单收益。
  - [ ] DOC-05c【未开始】将用户端发现、搜索、商品、购买、订单、钱包、我的、登录、AI 与状态等 V2 图组逐组转为 V3，并保持新四栏导航一致。
  - [ ] DOC-05d【未开始】将推广申请、工作台、推广位、转链、活动、订单、资产及异常状态等 V2 图组逐组转为 V3；保持现有安全 / 审核状态语义。
  - [ ] DOC-05e【未开始】将跨平台电商、本地生活与后台 V2 图组逐组转为 V3；后台不强套移动端底栏。
  - [ ] DOC-05c-1【未开始】用户端发现与多平台总览 V3：`user-ui-discovery-v2`、`mobile-ui-overview-v2`。
  - [ ] DOC-05c-2【未开始】用户端搜索 / AI / 购买 V3：`user-ui-ai-v2`、`user-ui-purchase-v2`。
  - [ ] DOC-05c-3【未开始】用户端订单 / 钱包 / 我的 V3：`user-ui-orders-v2`、`user-ui-wallet-v2`、`user-ui-profile-v2`。
  - [ ] DOC-05c-4【未开始】用户端登录 / 通用状态 V3：`user-ui-auth-v2`、`user-ui-states-v2`。
  - [ ] DOC-05d-1【未开始】推广申请、工作台与推广位 V3：`promoter-onboarding-positions-v2`、`promoter-states-onboarding-v2`。
  - [ ] DOC-05d-2【未开始】推广转链及异常结果 V3：`promoter-convert-share-v2`、`promoter-states-convert-v2`、`promoter-states-result-v2`。
  - [ ] DOC-05d-3【未开始】推广活动、订单、收益与结算 V3：`promoter-activities-orders-v2`、`promoter-wallet-settlement-v2`。
  - [ ] DOC-05e-1【未开始】平台电商 / 本地生活 V3：`expanded-ecommerce-platforms-v2`、`local-life-travel-platforms-v2`、`multi-platform-client-overview-v2`。
  - [ ] DOC-05e-2【未开始】后台总览 V3：`admin-dashboard-overview-v2`，沿用色彩和卡片语言，不使用 H5 底导航。
  - `home-promoter-entry-v2` 已由 DOC-03 的 `home-platform-modules-h5-v3` 替代；`user-ui-discovery-v2` 的旧省钱频道由 DOC-05a 的领券一级页先行替代，完整旧图组仍待 DOC-05c-1。以上任务覆盖现存全部 21 组 V2 PNG，不原地覆盖历史稿。
- 已有 Go HTTP / Tracking / 京东适配边界、转链服务、PostgreSQL Tracking 迁移及仓储测试，可复用；不是完整推广中心。
- `cmd/api` 当前仅接健康检查；身份、生产依赖装配、真实渠道调用及推广前端不能视为完成。
- 推广身份模型、申请持久化、提交与查询接口已完成独立测试，尚未接入生产认证和启动装配；推广位归属、预览、推广订单、分账和结算仍待开发。

### 历史设计归档记录

- [x] PROC-01【已完成】固化逐任务状态及 Git 提交约定。完成日期：2026-09-16；变更：根目录 AGENTS.md、任务编号和状态记录规则；验证：289 个本地文档链接通过，`git diff --check` 通过；限制：仅本地提交，未推送远端。提交以 `PROC-01` 查询 Git 历史。

DOC-01 / DOC-02｜2026-09-16｜此前已完成的首页与推广中心图稿、详细设计及跨文档关联，本次首次统一归档｜本地 Markdown 链接检查、`git diff --check`｜仅文档与图稿，不含研发实现；此前工作没有逐项提交，不能补造历史提交。此归档后新增研发任务严格逐项提交。

## 依赖与里程碑

M0 规则 / 工程准备 → M1 身份与推广位 → M2 预览转链 → M3 订单归因 → M4 结算钱包 → M5 活动扩展。渠道权限验证可与 M0 / M1 同期推进，但 M2 真实验收依赖获批京东能力。每个里程碑均包括服务端、用户端、测试与文档更新。

### M0：决策、渠道与交付基础

- [ ] M0-01【未开始】产品 / 财务确认自购奖励互斥、匿名消费者返现、统一钱包、负余额追偿、旧链接停用和提现规则；记录版本与审批人。
- [ ] M0-02【进行中】首期先落地仓库内 H5，采用原生 HTML/CSS/ES Modules 并由现有 Go API 同源提供；工程位置为 `web/`，无需额外前端构建依赖。小程序范围、真实登录提供方和正式发布方案仍待确认；不把图稿当现有前端实现。
- [ ] M0-03【未开始】验证已轮换渠道凭据、账户 / 推广位权限与接口版本；凭据仅进入密钥管理，不进仓库。
- [ ] M0-04【进行中】完成运行时配置、鉴权身份解析、PostgreSQL 仓储及渠道依赖装配，明确测试模拟与生产开关。
  - [x] M0-04a【已完成】2026-09-16：新增 `cmd/migrate` 与 `internal/dbmigrate`，按编号执行 up SQL、记录 SHA-256、拒绝已应用文件被修改 / 丢失和事后插入更早版本；使用 PostgreSQL advisory lock 序列化并发执行，每个迁移与账本记录在同一事务。真实 PostgreSQL 16 测试四路并发、重复运行、五份现有迁移、摘要偏差、缺失 / 乱序历史及失败回滚；`go test -count=1 ./...`、`go vet ./...` 通过。仅在随机隔离 schema 验证，没有对业务数据库执行迁移，也未装配生产认证。
  - [x] M0-04b【已完成】可信用户 / 管理员认证解析与推广模块数据库依赖装配，缺失配置安全关闭；代码与测试完成，真实身份提供方的签发配置仍需部署验收。
    - [x] M0-04b-1【已完成】2026-09-16：新增 RSA 2048+ 公钥访问令牌验证器，只接受 RS256 且 typ=at+jwt，校验签名、iss、aud、exp、nbf、sub，并限制授权头长度；从已验签 sub 解析用户 ID，不信任请求参数或角色字段。Go 1.26 / PostgreSQL 16 全量 `go test -count=1 ./...` 与 `go vet ./...` 通过，含伪造签名、算法、错误用途 / 签发者 / 受众和过期用例。需要实际提供该格式令牌的身份提供方及公钥配置；尚未接入 API 启动入口。
    - [x] M0-04b-2【已完成】2026-09-16：新增 000006 管理员账号 / 权限迁移和数据库授权查询；管理员以已验签的同一 sub 为 ID，每次请求按启用状态读取 `promoter:read/review/disable` 与 `channel:position:configure` 权限，令牌自带角色字段无效。真实 PostgreSQL 验证未授权、授予、停用和撤权，迁移全链路自动识别第六版；`go test -count=1 ./...`、`go vet ./...` 通过。管理员账号仍需运营在受控流程中开通，启动入口尚未装配。
    - [x] M0-04b-3【已完成】2026-09-16：`cmd/api` 装配已验签用户身份、数据库管理员权限、申请 / 审核 / 推广位路由与 PostgreSQL 仓储；启动前检查数据库连接、所有迁移摘要、签发方 / 受众 / 公钥及协议版本，缺项启动失败。真实 PostgreSQL 集成测试以签名令牌验证申请→审核→推广位，数据库撤权立即生效；管理员授权数据库故障返回 503。Go 1.26 / PostgreSQL 16 执行 `go test -count=1 ./...`、`go vet ./...` 通过。真实签发方和获批协议仍需环境配置，京东正式客户端未装配。
  - [ ] M0-04c【未开始】获批渠道客户端配置、测试 / 生产开关和完整启动验证。
- [ ] M0-05【进行中】补绘审核中 / 拒绝 / 停用、空数据、未配置渠道、过期与重试状态；补审核后台交互稿。图稿清单与 P 编号互链。
  - [x] M0-05a【已完成】2026-09-16：按既有 V2 视觉补绘 P-01 审核中 / 拒绝、P-02 资格停用、P-03 无推广位四屏状态图，并在用户端图册、详细设计和图片索引中互链；逐屏注明可操作项与服务端权限，待审/拒绝/停用不显示可用转链，无推广位仅引导新建且提示京东仍待配置。内置 imagegen 生成后检查并定向移除误生成的第三方吉祥物图标，保存生成提示词与修正记录。验证：最终 PNG 可读取、图稿链接存在、`git diff --check` 通过。仅设计概念稿，图中申请时间为演示，前端开发与真实接口状态联调尚未完成。
  - [x] M0-05b【已完成】2026-09-16：沿用 V2 视觉补绘 P-05 京东渠道待配置、P-06 预览过期 / 价格规则变化、P-07 生成中 / 结果待核验 / 确定失败 / 展示有效期结束共七种状态；在用户端图册、详细设计和图片索引中互链并逐项注明按钮与服务端边界。结果不确定只查询原请求不重发，确定失败后重新预览并使用新请求；过期展示不擅自断言外部渠道链接已停用。内置 imagegen 生成后定向修正过期文案，保留提示词记录。验证：两张最终 PNG 可读取、文档链接存在、`git diff --check` 通过。仅视觉与交互设计，真实渠道能力和前端实现未完成。
  - [ ] M0-05c【未开始】补后台审核与渠道位配置交互稿、权限与审计提示。
  - [ ] M0-05d【未开始】补订单、看板、链接历史等其他空数据页面状态与加载 / 错误组件。

验收：规则待决项有明确阻断范围；前端可启动；真实用户身份不可由请求任意指定；缺少渠道配置时安全关闭而非生成假成功。

### M1：首页入口、申请与推广位（P-00～P-04）

- [ ] M1-01【进行中】按 P-00 V3 图稿实现 H5 多平台首页：京东、美团、淘宝等平台可切换，选中后显示该平台的推广商品、优选商店、领券中心、推广专区；底栏固定首页 / 领券 / 推广 / 我的四项，「我的」保留推广中心次入口。验收覆盖平台切换、导航去向、窄屏与空 / 加载 / 错误 / 未开通状态；演示商品和券不得进入生产数据。真实渠道内容另按获批能力验收。
  - [x] M1-01a【已完成】2026-09-17：新增 Go 同源提供的 `/app/` H5 首页，使用「万宝单生活」名称、平台切换、四模块与首页 / 领券 / 推广 / 我的四栏导航；首页券模块从只读目录取真实数据，覆盖加载、无数据、错误和未开通状态，商品 / 店铺 / 推广模块保持明确空态。验证：嵌入资源及运行时 HTTP 测试、`node --check web/app.js`、Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...` 与 `go vet ./...` 通过，本地 H5 返回 200。限制：登录、真实商品 / 店铺、推广内容和领券动作仍未接通；仅首页基础验收。
  - [ ] M1-01b【未开始】接入获批商品 / 店铺及推广内容、完整登录与身份分流，完成响应式与真实端到端验收；未获批平台保持未开放。
  - [x] M1-01c【已完成】2026-09-19：统一 uni-app 的 V3 首页四模块、五个平台上下文及首页 / 领券 / 推广 / 我的四栏；首页与领券页共享平台、切换清除旧提示，入口使用 switchTab；「我的」保留订单 / AI 二级入口，订单提供支持 tap / Enter / Space 的应用内返回。未接客户端接口的模块显示明确原因，不复用图稿商品 / 券。相关导航与平台测试先实际失败，审查发现订单缺少返回后新增三项失败测试再修正；`npm test`（49 项）、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`git diff --check` 通过，只读复审无阻断。Browser 插件未提供，现有 Playwright / Chrome 在 127.0.0.1:5173 验证 320×844、390×844、1280×844 和概念稿原尺寸 891×1774：页面标题 / 非空 / 无框架覆盖 / 无运行错误 / 无整页横向溢出，平台 tap / Space、首页→领券平台保留→换平台→底栏 Space 返回首页、推广入口及我的→订单→Space 返回、AI 往返均通过。仅结构及本地交互验收，真实券目录与地域读取另由 M1-01d 验收；M1-01 整体、微信真机和真实业务未完成。
    - 图稿核对记录：同一轮直接查看 `home-platform-modules-h5-v3.png` 与最新浏览器截图；① 四模块顺序和四栏标签对齐；② 京东红色胶囊 / 下划线选中、薄荷英雄区、白色圆角模块及红橙绿蓝标题标记沿用；③ 搜索与进入推广为真实组件，纸飞机 / 券 / 人物导航图标与图稿同类；④ 当前品牌按 DOC-08 改为万宝单生活，首屏副标题改为授权后展示，追加未接入状态，删除演示商品 / 券额 / 无实际分享结果的 CTA；⑤ 未提供授权素材，不复制京东吉祥物或商品图片，留作后续视觉与获批内容验收；⑥ 320px 搜索独立一行、平台可水平滚动，宽屏保持客户端 520px 居中，手机外框 / 系统栏不在网页内实现。文案、图像、空态密度与图稿仍有这些有意差异，不宣称像素级复刻或最终 V3 视觉验收通过。
  - [x] M1-01d【已完成】2026-09-19：完成 uni-app 首页与领券页的只读券目录、地域、详情 API 接入，以及平台 / 地域 / 业务切换失效保护。下列两项记录契约与页面验证；仅本地模拟及 HTTP 测试服务验收，不代表真实渠道、数据库、领取购买或微信真机验收，M1-01 整体仍未完成。
    - [x] M1-01d-1【已完成】2026-09-19：新增 `apps/client/src/features/coupons/api.ts` 与 `tests/coupon-api.test.ts`，UI 平台映射 JD / TB / MT、PDD / ELEME 不发请求；只读目录 / 地域 / 详情 GET，查询与路径编码、10 秒 uni 请求超时、HTTP / 成功信封 / 完整字段 / 安全整数 / 当前平台地域业务 / 过期及领取模式文案校验，区分券不存在与服务未接入，错误脱敏。24 项先实际失败再实现；后续过期与动作标签两项、严格枚举 / 404 分类 / 合法无平台前缀 ID 与可空名称四项回归实际失败后修正，共 34 项契约测试。`npm test`（全量 83 项）、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test ./internal/httpapi ./internal/coupon`、`git diff --check` 通过，只读复审无阻断。更新客户端 README 明确传输契约与页面边界。未配置 PG_TEST_DSN，Go 数据库集成跳过；仅模拟传输与编译，不连接页面、不签发身份、不调用领取 / 购买、不代表真实渠道、实际数据库或微信设备验收，后续页面与请求失效由 M1-01d-2 完成。
    - [x] M1-01d-2【已完成】2026-09-19：新增共享目录状态与券目录组件，接入首页两条摘要与领券页地域 / 业务筛选、详情、分页重试及加载 / 空 / 错误 / 未开放状态。上下文改变清空旧数据、详情与游标，忽略旧响应；初次地域失败仍可读取不限城市活动，暂时错误不改变已有明确城市选择。新详情更新缓存规则与有效期，到期或详情确认失效后隐藏缓存券，保留失效说明与剩余分页。开发代理仅开放本机 8080 的券只读路径，不新增领取 / 购买。目录、组件、页面及过期 / 新详情 / 失效缓存回归先实际失败再修正。验证：`npm test` 全量 109 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过，只读复审无阻断。Playwright / Chrome 在 127.0.0.1:5173 经实际 uni 请求和 Vite 代理读取临时本机 HTTP 测试服务，在 320 / 390 / 1280 屏宽验证上下文、规则、分页失败重试、旧请求失效、到期隐藏、空 / 错误 / 未开放、键盘及四栏导航：56 次只读无授权请求，刻意触发的 9 条 404 / 503 日志可解释，无其他运行错误或整页横向溢出。未设置 PG_TEST_DSN，数据库集成跳过；仅本地测试数据验收，非真实渠道、现有数据库、微信真机、领取购买或生产部署验收。
- [ ] M1-02【进行中】实现登录回跳与未申请、待审、拒绝、已启用、停用分流，补完整加载和错误状态。
  - [x] M1-02a【已完成】2026-09-18：将 H5 推广占位页改为状态入口；未登录时明确提示登录尚未接入、不能申请或转链，并可返回领券页。新增纯状态组件，按已验证令牌的本人资料投影未申请 / 待审 / 拒绝 / 已启用 / 停用；已启用时可读取本人推广位和基础看板，渠道未就绪时禁用生成。验证：`node --test web/promoter.test.mjs web/share.test.mjs`、`node --check web/app.js`、`node --check web/promoter.mjs`、`go test -count=1 ./web`、`git diff --check` 通过；本地浏览器确认未登录页和返回领券导航、宽屏样式。限制：现有 H5 尚无真实令牌来源，运行时只展示未登录态；其他状态仅注入测试，真实登录、回跳与终端验收待 M1-02b。
  - [ ] M1-02b【未开始】接入真实手机号 / 微信身份提供方、令牌会话及登录回跳，完成端到端身份分流，并把令牌接入 M1-02a 状态组件。
- [ ] M1-03【进行中】新增推广身份 / 申请迁移及仓储、申请 / 状态接口；拆分为以下独立验收子任务，整体尚未完成。
  - [x] M1-03a【已完成】推广身份、申请状态与权限领域模型；2026-09-16：新增 `internal/promoter`，验证同意协议、归属、合法状态转换、版本冲突及停用边界。Go 1.26 容器执行 `go test ./...`、`go vet ./...` 通过；不包含数据库持久化、后台授权及渠道权限验证，数据库集成测试未配置 DSN，未作为本任务验收。
  - [x] M1-03b【已完成】本人推广状态查询服务与 HTTP 接口；2026-09-16：新增查询服务、仓储边界及 `GET /api/v1/promoter/profile` 依赖注入路由。Go 1.26 容器执行 `go test ./...`、`go vet ./...` 通过；覆盖所有身份状态、未申请默认值、越权防护、鉴权失败、读取错误脱敏和缺依赖关闭路由。仅以测试仓储 / 身份验证接口，尚未接入生产认证、PostgreSQL 或 `cmd/api` 启动装配。
  - [ ] M1-03c【进行中】PostgreSQL 迁移及仓储、申请提交接口与事务幂等；按以下子任务验收，整体仍未完成。
    - [x] M1-03c-1【已完成】2026-09-16：新增身份 / 申请迁移及 PostgreSQL 仓储，行锁串行化同人申请，同人同键同输入重放、不同输入冲突；申请与身份同事务。Go 1.26 + PostgreSQL 16 设置 PG_TEST_DSN 执行 `go test -count=1 ./...`、`go vet ./...` 通过。验证 12 路多连接同键 / 不同键并发、跨用户键隔离、失败回滚、up/down/up 迁移。测试仅使用随机独立 schema，未对业务库应用迁移；后台审核和运行时装配仍待完成。
    - [x] M1-03c-2【已完成】2026-09-16：实现申请提交服务及 POST 接口，校验明确同意和服务端配置的协议版本；身份来自认证、时间和随机 ID 来自服务端，限制 4 KiB JSON、字段长度、未知字段和尾随数据。真实 PostgreSQL 下验证规范化重放、审核后旧键重放与拒绝后新键重申；`go test -count=1 ./...`、`go vet ./...` 通过。尚未提供真实协议内容和生产身份装配，不能直接开放申请。
    - [ ] M1-03c-3【未开始】配合 M0-04 接入真实认证及运行时数据库依赖，验证完整启动链路。
    - [x] M1-03c-4【已完成】2026-09-16：实现 GET current 接口、查询服务及单条 JOIN 快照读取，返回审核状态、用户可见原因与重申权限；身份只取认证上下文，未申请 404，其他用户不可查询，内部通过备注不外泄。真实 PostgreSQL 验证查询与用户隔离，HTTP 覆盖各状态与安全错误；`go test -count=1 ./...`、`go vet ./...` 通过。仍需 M1-03c-3 运行时认证装配。
- [ ] M1-04【进行中】实现后台申请审批、停用、权限与审计；推广员不能自助改变审核结果。
  - [x] M1-04a【已完成】2026-09-16：新增审核 / 停用授权服务及 000003 审计迁移，按管理员 + 操作 + 幂等键锁定请求，身份 / 申请状态与审计同事务；禁止自审，版本冲突拒绝，审计禁止 UPDATE / DELETE。Go 1.26 + PostgreSQL 16 执行 `go test -count=1 ./...`、`go vet ./...` 通过，覆盖 12 路重复审核 / 不同键竞争、停用后旧回执重放不改变现状、审计失败回滚与迁移 up/down/up。仅隔离 schema 测试，真实管理员身份仍未装配。
  - [x] M1-04b【已完成】2026-09-16：新增后台申请审核和推广员停用 POST 接口；独立 AdminResolver 提供服务端身份与权限，HTTP 和服务层均校验操作权限。必须明确审核决定、版本、原因和幂等键，限制 JSON 体积并拒绝客户端伪造身份字段。`go test -count=1 ./...`（含真实 PostgreSQL）与 `go vet ./...` 通过；普通用户认证不注册管理路由，缺依赖安全关闭。生产管理员认证未装配。
  - [ ] M1-04c【进行中】后台申请列表、审核页面、真实管理员认证与运行时装配。
    - [x] M1-04c-1【已完成】2026-09-16：新增后台当前申请列表 GET 接口、读取权限 promoter:read、状态筛选、1～100 条分页及时间 / ID 双字段游标，返回身份版本供审核使用。游标绑定状态筛选，同时间申请按 ID 稳定排序；每人仅当前申请。真实 PostgreSQL 验证多页无重复、筛选及空列表，HTTP 验证认证权限、重复 / 非法参数、错误脱敏；`go test -count=1 ./...` 与 `go vet ./...` 通过。分页不是跨请求冻结快照，页面提交前仍以版本冲突保护；后台页面与生产认证未完成。
    - [ ] M1-04c-2【未开始】后台页面、真实管理员认证及运行时装配，完成端到端验收。
- [ ] M1-05【进行中】新增内部推广位及渠道映射、创建 / 编辑 / 默认 / 停用接口；推广位仅本人可见，外部渠道配置由受权后台操作。
  - [x] M1-05a【已完成】2026-09-16：新增内部推广位与操作幂等迁移、服务和 PostgreSQL 仓储。写操作锁定推广身份，只有已启用用户可变更；默认切换同时递增旧默认位版本，停用清除默认但保留记录。真实 PostgreSQL 验证 12 路同键创建、并发默认创建 / 切换、跨用户隔离、版本冲突、回执失败回滚、游标分页、停用身份只读历史及迁移 up/down/up；`go test -count=1 ./...`、`go vet ./...` 通过。尚未关联真实渠道位或启动装配。
  - [x] M1-05b【已完成】2026-09-16：新增本人推广位列表 / 创建 / 编辑 / 默认 / 停用五个接口，只使用鉴权身份，拒绝前端伪造归属及渠道状态，限制请求体与动作字段。响应 JD=WAITING_CONFIGURATION、canConvert=false，不把内部位当已获批渠道位。HTTP 覆盖成功路径、未登录、跨用户依赖数据、非法字段 / 分页、超大请求、版本 / 幂等 / 身份错误脱敏；真实 PostgreSQL 全量回归 `go test -count=1 ./...` 及 `go vet ./...` 通过。尚未生产装配。
  - [ ] M1-05c【进行中】受权后台渠道位映射与真实 readiness，用户端页面及运行时装配。
    - [x] M1-05c-1【已完成】2026-09-16：新增京东渠道位映射与追加型配置审计迁移，受 `channel:position:configure` 权限保护；按管理员 / 幂等键返回同一回执、不同输入冲突，验证推广身份及内部位可用，同一渠道账户 / 外部位禁止重复绑定。仅允许 `PENDING_VERIFICATION`，管理员填入外部 ID 不能自行设为 READY。PostgreSQL 16 独立 schema 验证 12 路并发重试、版本冲突、跨位重复绑定、停用限制、审计不可修改、审计失败回滚和 up/down/up；`go test -count=1 ./...`、`go vet ./...` 通过。尚无真实渠道账户核验证据或生产装配。
    - [x] M1-05c-2【已完成】2026-09-16：用户端推广位列表通过联表读取京东渠道位映射，分别显示 WAITING_CONFIGURATION、WAITING_VERIFICATION、UNAVAILABLE；推广身份或位停用时不可用。新建 / 修改操作回执同样按现有映射投影，`canConvert` 仍为 false。真实 PostgreSQL 验证未配置 → 映射待核验 → 身份停用，HTTP 验证待核验映射不开放转链；`go test -count=1 ./...`、`go vet ./...` 通过。无真实渠道核验证据，不返回 READY。
    - [ ] M1-05c-3【进行中】渠道管理员配置接口、真实渠道账户核验、管理员页面及运行时装配；取得渠道权限证据后才允许 READY。
      - [x] M1-05c-3a【已完成】2026-09-16：新增渠道管理员京东位 PUT 配置接口；仅 AdminResolver 提供的 `channel:position:configure` 身份可调用，拒绝前端传入 READY、归属或未知字段，限制请求体与幂等键，按目标不存在 / 版本 / 外部位重复绑定映射稳定错误。HTTP 注入测试与真实 PostgreSQL 全量测试执行 `go test -count=1 ./...`、`go vet ./...` 通过。接口仅写待核验配置，真实京东权限验证及生产启动仍待完成。
      - [ ] M1-05c-3b【未开始】真实京东账户 / 推广位核验与失效机制，保留脱敏证据。
      - [ ] M1-05c-3c【未开始】管理员页面及生产认证、数据库、迁移装配。
- [x] M1-06【已完成】2026-09-16：新增真实 PostgreSQL + HTTP 的跨模块集成测试，串联未申请、同键申请重放、待审列表、审核、默认位切换、过期版本、渠道位待核验、跨用户隔离、推广员停用与历史保留，并核对申请 / 推广位 / 审计条数。既有 PostgreSQL 用例另覆盖 12 路并发默认位操作及停用默认位。Go 1.26 / PostgreSQL 16 下 `go test -count=1 ./...`、`go vet ./...` 通过。测试通过注入身份依赖运行；不证明生产认证、京东核验或真实转链已接通。

验收：所有入口状态可复现；最多一个启用默认位；渠道待配置不可转链；停用不删除订单和历史收益。

### M2：预览、转链与分享（P-05～P-08）

- [ ] M2-01【未开始】扩展 `internal/promotion`、`internal/tracking`、`internal/channel/jd` 和 HTTP 路由，新增兼容历史记录的迁移。
- [ ] M2-02【进行中】实现安全识别与只读预览接口，商品 / 佣金估算包含更新时间、规则版本和过期时间。
  - [x] M2-02a【已完成】2026-09-16：新增严格渠道 URL 策略，必须配置精确 ASCII 主机白名单，只接 HTTPS / 443，拒绝用户信息、片段、异常查询、IP 字面量及后缀伪装；公网地址策略拒绝环回、私网、链路本地、运营商级 NAT、测试网和转换网段。Go 1.26 + PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...` 通过。域名清单需以渠道正式授权信息配置；尚未执行 HTTP 请求或接入预览。
  - [x] M2-02b【已完成】2026-09-16：新增受限 HTTPS GET 展开器，逐跳检查精确域名白名单，连接前解析全部 DNS 地址并拒绝混合私网结果，按验证后的公网 IP 拨号且保留 TLS 主机名校验；禁用代理与连接复用，最多 3 次跳转、5 秒总超时、64 KiB 响应体和 8 KiB 响应头。使用本地证书服务器验证合法跳转、跳向未授权域名、DNS 重绑定 / 混合解析、证书域名不符、超大响应和循环跳转；`go test -count=1 ./...`、`go vet ./...` 通过。尚未接入京东商品解析或对外预览接口。
  - [ ] M2-02c【进行中】接入获批渠道商品 / 佣金数据，保存只读预览并暴露接口；获批渠道数据及生产凭据尚未取得。
    - [x] M2-02c-1【已完成】2026-09-16：新增只读预览快照迁移及仓储，数据库以复合外键约束推广位归属、按用户与幂等键唯一，并保存商品、分栏估算金额、币种、规则版本、渠道证据引用、更新时间和过期时间；不创建链接或资金记录。真实 PostgreSQL 16 验证 12 路同键并发、同键异指纹冲突、跨用户键隔离与越权读取，Go 1.26 执行 `go test -count=1 ./...`、`go vet ./...` 通过。当前仅内部存储边界；渠道真实报价、身份 / readiness 校验和 HTTP 接口仍待 M2-02c-2/3，未开放用户使用。
    - [x] M2-02c-2【已完成】2026-09-16：新增 `POST /api/v1/promotions/preview` 严格 JSON/大小/幂等键校验，身份仅来自已验签令牌；服务检查本人已启用身份、推广位、京东渠道 READY，安全展开链接后才向注入的获批报价器询价，保存前再次检查资格。同键同输入回放既有预览，不重复询价；不同输入冲突。响应分栏显示券后价、个人预计收益与消费者预计返现及更新时间、规则版本、过期时间，不暴露证据引用。生产入口已装配但未配置报价器、且当前渠道位只能待核验，所以返回未就绪/不可用，不写模拟报价。真实 PostgreSQL + HTTP 验证未登录、待核验 409、零预览写入；注入测试覆盖重放、错误映射与请求伪造。Go 1.26 执行 `go test -count=1 ./...`、`go vet ./...` 通过。获批域名/京东报价接入、READY 核验证据与端到端真实成功路径仍待 M2-02c-3。
    - [ ] M2-02c-3【未开始】接入获批京东商品和佣金接口，以真实测试账户完成报价证据和端到端验收。
- [ ] M2-03【进行中】实现转链接口：幂等指纹、推广位归属、渠道 readiness、服务端二次校验、持久化状态与失败恢复。
  - [x] M2-03a【已完成】2026-09-16：新增 000008 转链请求迁移和 `internal/conversion` 原子预留仓储。新请求与既有 Tracking 同事务写入，三元外键绑定预览 / 用户 / 推广位，保存场景、请求指纹和 PENDING 状态；历史购物 Tracking 不改写。同用户同键同指纹重放原请求，异指纹冲突，停用身份 / 位及过期预览不得新建。真实 PostgreSQL 16 验证 12 路并发仅一条请求与 Tracking、跨用户隔离、失败回滚；Go 1.26 执行 `go test -count=1 ./...`、`go vet ./...` 通过。仅内部预留边界，尚未接 HTTP、渠道 readiness 二次校验或调用真实京东转链，不代表链接已生成。
  - [x] M2-03b【已完成】2026-09-16：新增鉴权的 `POST /api/v1/promotions/convert` 和转链服务，拒绝前端提交价格 / 归属字段，按本人预览、推广位、场景和幂等键计算请求指纹；创建前校验已启用身份、渠道 READY、预览有效期，调用可注入的获批渠道复核器比较商品、券后价、个人预计收益、消费者预计返现和规则版本，再次核验资格后才原子预留 Tracking / 转链请求。同键重放不重复复核，不同请求冲突；预留成功仅返回 202 PENDING，不宣称已生成链接。生产未装配复核器且渠道仍待核验，接口安全返回 409/503，不写 Tracking。真实 PostgreSQL + HTTP 验证生产安全关闭、价格变化零写入和匹配报价后单条预留；注入测试覆盖资格在复核后失效。Go 1.26 `go test -count=1 ./...`、`go vet ./...` 通过。真实渠道复核、READY 核验、转链状态查询和超时恢复仍待 M2-03c；当前资格重查与预留不是同一数据库快照，最终开放前需将渠道状态在预留事务内再次校验。
  - [ ] M2-03c【进行中】获批渠道转链调用、请求状态机、超时查询恢复与真实端到端验收。
    - [x] M2-03c-1【已完成】2026-09-16：新增本人 `GET /api/v1/promotions/convert/{id}` 状态接口和 POST 202 回执的 `statusUrl`；按已验签身份及数据库归属读取，跨用户同 ID 返回 404，已停用推广员仍可查历史。PENDING / PROCESSING 不暴露链接，仅 SUCCEEDED 响应持久化链接；同键重放复用状态。真实 PostgreSQL 验证停用后读取、跨用户隔离和状态字段，HTTP 验证鉴权、404 / 503 错误脱敏与链接仅成功可见；Go 1.26 `go test -count=1 ./...`、`go vet ./...` 通过。当前生产无可成功执行的渠道客户端，状态仍只能停留在 PENDING；调用、恢复和真实验收待 M2-03c-2/3。
    - [ ] M2-03c-2【进行中】渠道调用状态机、超时查询恢复和幂等结果归档；未获批时不得启用生产调用。
      - [x] M2-03c-2a【已完成】2026-09-16：新增 000009 转链状态迁移，记录版本、尝试次数、处理租约和脱敏失败码；持久化状态方法以版本比较原子领取 PENDING，请求号固定为内部请求 ID，处理期间停用身份 / 推广位不可新领取。超时不确定结果转为 FAILED_RETRYABLE / QUERY_REQUIRED，恢复候选仅读取待查询项而不自动重发；确定失败与成功按请求号和版本归档，终态不可回退。成功链接要求受限 HTTPS 域名格式，仍须后续获批渠道域名校验。真实 PostgreSQL 16 验证 12 路并发仅一个领取、租约过期候选、迟到成功、重复/非法转换及 up/down/up；Go 1.26 `go test -count=1 ./...`、`go vet ./...` 通过。未装配执行器或渠道客户端，生产不会调用京东；查询恢复与渠道幂等保证待 M2-03c-2b/3。
      - [x] M2-03c-2b【已完成】2026-09-16：新增仅供内部调用的转链执行器边界，不装配 `cmd/api`。执行前按本人推广位 readiness、预览有效期及渠道复核报价再检查；价格变化在调用前终止。CAS 领取后用固定请求号向可注入的渠道 Gateway 创建一次，确定拒绝归档 FAILED_FINAL，超时/未知结果归档 QUERY_REQUIRED；恢复流程只用同一请求号查询渠道，找到成功或确定拒绝后归档，UNKNOWN 不重发创建。渠道返回链接须由 Gateway 按获批域名判断，仓储另限 HTTPS；持久化结果使用独立短超时上下文，避免调用者断开后遗失不确定状态。真实 PostgreSQL 验证 12 路并发仅一次创建、超时→未知→查询成功且创建次数仍为一、改价前置失败、确定拒绝、缺 Gateway 关闭；Go 1.26 `go test -count=1 ./...`、`go vet ./...` 通过。只有注入测试 Gateway，生产无执行器 / 真实查询能力；渠道位 READY 核验证据、渠道 SDK、真实端到端及预留事务内 readiness 校验仍待 M2-03c-3，未证明京东幂等能力，不开放自动重试。
    - [ ] M2-03c-3【未开始】已获批京东转链客户端、READY 证据与真实端到端验收。
- [ ] M2-04【进行中】完成京东正式客户端 / 签名 / 超时 / 脱敏及契约测试，保留现有购物导购接口兼容性。
  - [x] M2-04a【已完成】2026-09-17：实现 `jd.union.open.promotion.common.get` 商品推广的京东适配和服务端签名客户端，补参数 / 响应 / 错误边界契约测试，并更新京东接口文档；本里程碑仅指客户端代码及模拟契约通过，不代表真实渠道已开通。账号密码和授权 Key 未进入仓库。
    - [x] M2-04a-1【已完成】2026-09-17：新增商品编号 / 标准京东商品 URL 规范化，编号以字符串保存；既有适配器保留方法签名和原始商品字段，增加 MaterialID，拒绝缺失客户端 / 推广位、非法商品、启用子标识但无 Tracking 及已取消请求。渠道错误普通输出脱敏，新增本地密钥忽略规则，并扩展京东接口文档，账号仅保留脱敏索引。Go 1.26 容器先执行 `go test -count=1 ./internal/channel/jd` 通过；全量测试首次因临时容器下载依赖 TLS 超时未完成，复用项目已有 Go 缓存并设置 PostgreSQL 16 的 `PG_TEST_DSN` 后，`go test -count=1 ./...`、`go vet ./...` 和 `git diff --check` 均通过。未调用真实京东、未装配生产 HTTP 客户端，子标识与推广位权限仍需账户核验。
    - [x] M2-04a-2【已完成】2026-09-17：依据登录后的京东联盟官方接口详情和新版接入说明，实现 `api.jd.com/routerjson` GET、参数排序大写 MD5、上海时区、`360buy_param_json.promotionCodeReq` 的单品 `sceneId=2`、要求配置实际获批标记、siteId、可选推广位数值与获批子标识、响应 clickURL 与错误分类；拒绝明文 / 非京东点击域名、跨域重定向和重复创建。使用本地 TLS 服务器测试签名向量、参数结构、对象 / 字符串内层、非法参数零请求、渠道拒绝 / 网络未知与安全 URL。Go 1.26 + PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过。控制台核对用户所给授权 Key 是第三方查询授权凭据，不是 AppKey/AppSecret；账号当前无应用或备案网站 / APP，未做线上调用、真实转链或渠道位 READY 核验。
  - [x] M2-04b【已完成】2026-09-17：新增仅供受控联调的 `cmd/jd-promotion` 单品命令，从服务端环境读取 AppKey/AppSecret、合规 siteId 与场景 2 / 子标识授权开关，复用京东适配器，要求明确商品和京东外部位，仅在安全 HTTPS 链接成功时输出；缺凭据、未获批子标识、非法商品、非数值推广位均安全关闭。文档加入配置、使用边界与验收命令。Go 1.26 + PostgreSQL 16 的 `go test -count=1 ./...`、`go vet ./...` 和 `git diff --check` 通过。当前账号无应用 / 网站 / APP，不存在可用 AppKey/AppSecret 或合规 siteId，未作真实京东调用，生产用户接口仍不装配。
  - [ ] M2-04c【阻塞】真实京东商品转链验收：官方协议与账户凭据类型已核对，但该账号尚无开放平台应用或网站 / APP 媒体，缺 AppKey/AppSecret、合规 siteId、场景 2 权限与已核验推广位。下一步由业务明确实际推广网站备案域名或 APP 包名及投放来源，在京东申请 / 审核媒体与场景 2 权限后，配置密钥并以真实商品调用受控入口，保存脱敏成功证据与打开结果。模拟契约测试不能替代真实成功，也不得提前将渠道位标为 READY。
- [ ] M2-05【进行中】用户端完成粘贴、确认商品、生成中、结果、链接 / 文案复制；QR 和海报未实现前显示未开放。
  - [x] M2-05a【已完成】2026-09-18：新增 H5 推广转链组件，只有已启用身份且本人京东推广位返回 `READY` / `canConvert=true` 时开放；主动粘贴后先预览商品、券后价和两类预计收益，再确认生成。PENDING / PROCESSING / 结果待确认只查询原请求；提交超时保留原幂等键并锁定输入，改内容 / 推广位会清空旧预览，过期预览不提交转链。仅 SUCCEEDED 开放链接 / 文案复制，QR 与海报显示未开放。验证：`node --test web/promotion-flow.test.mjs web/promoter.test.mjs web/share.test.mjs`、`node --check` 四个模块、`go test -count=1 ./web`、`go vet ./web`、`git diff --check` 通过；覆盖生成中不复制、同键重试、输入锁定、过期拒绝及旧结果清理。限制：当前生产无真实登录和 `READY` 渠道位，可操作界面尚无法真实到达；模拟契约通过不代表真实渠道、终端剪贴板或订单验收，待 M2-05b。
  - [ ] M2-05b【未开始】真实登录与渠道能力接通后完成浏览器及真实业务端到端验收。
- [ ] M2-06【进行中】分享事件去重，复制失败不报成功；公开内容不暴露个人收益。
  - [x] M2-06a【已完成】2026-09-18：新增本人 `SUCCEEDED` 转链的 `GET /api/v1/promotions/convert/{id}/share-artifacts?type=link|text`；文案只含商品标题、成功链接及平台结算提示，不返回个人预计收益、消费者返现或渠道证据。非本人 404、未成功 409，QR / 海报请求 400；服务端再次校验已存链接的 HTTPS 结构。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过，覆盖归属、状态、非法 URL、错误脱敏及未开放类型。限制：生产尚无获批成功转链；文案复制、事件去重和真实分享验收待 M2-06b，链接渠道域名最终由获批适配器验证。
  - [ ] M2-06b【进行中】分享 / 复制操作事件去重及 H5 成功后上报；未确认复制不记成功，不将事件解释为送达或成交。
    - [x] M2-06b-1【已完成】2026-09-18：新增 `POST /api/v1/promotions/convert/{id}/share-events`，认证用户仅可对本人 `SUCCEEDED` 转链上报 `COPY_REPORTED`；同一用户事件号并发去重，重复相同内容返回原记录、不同内容返回 409。只存事件号、转链编号、操作、内容类型、场景和记录时间；不存复制文案、收件人或成交状态。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过，覆盖并发、归属、未成功状态、冲突、非法请求字段和错误脱敏。限制：接口接收客户端上报，不能证明系统剪贴板写入、消息送达或成交；H5 成功后上报及端到端验收待 M2-06b-2。
    - [ ] M2-06b-2【进行中】H5 实际复制成功后上报，失败不报成功，完成端到端验证。
      - [x] M2-06b-2a【已完成】2026-09-18：新增同源 H5 `share.mjs` 模块，从已认证会话传入令牌读取本人分享内容，仅在 `navigator.clipboard.writeText` 成功后用同一事件号上报；复制失败不调用事件接口，复制成功但上报失败分别返回状态，模块不保存令牌或分享文案。验证：`node --test web/share.test.mjs` 5 例、`node --check web/share.mjs`、`go test -count=1 ./web ./internal/httpapi`、`go vet ./web`、`git diff --check` 通过；Go 嵌入资源测试确认模块可访问。限制：尚无真实 H5 登录态、推广工作台和剪贴板终端验收，页面按钮待 M2-06b-2b，不能声称真实分享已完成。
      - [ ] M2-06b-2b【未开始】接入真实登录态与推广工作台，开放复制按钮并完成端到端验证。
- [ ] M2-07【进行中】验证 SSRF / 重定向 / DNS 地址变化、过期预览、价格变化、并发同键、同键不同推广位、超时恢复和越权读取。
  - [x] M2-07a【已完成】2026-09-18：调整转链服务先检查本人已有幂等键指纹，确保同键换推广位稳定返回冲突且不新增 Tracking，同时保留同输入重放时的资格复查。服务 / PostgreSQL 用例覆盖新推广位无效与跨用户位两种冲突情形；复核现有链接展开测试包含精确域名、跳转逐跳校验、DNS 重绑定 / 混合地址和 TLS 主机名，转链测试包含过期 / 改价、同键并发、未知结果仅查询与本人读取隔离。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过。限制：生产渠道 READY 位尚不可达，渠道状态在预留事务内的最终复查和真实终端安全验收仍待 M2-07b；现有模拟用例不能代替获批京东真实调用。
  - [ ] M2-07b【未开始】真实渠道和终端条件具备后完成端到端安全复核，并将渠道 READY 状态在预留事务内最终复查。
  - [x] M2-07d【已完成】2026-09-18：转链执行器在复核报价返回后重查预览有效期，并在领取执行租约后、发送渠道创建请求前重查预览与报价有效期；过期记录终止为 `FAILED_FINAL / INVALID_RESULT`，不调用渠道创建。隔离 PostgreSQL 16 用例覆盖复核期间预览过期、领取锁等待期间预览或报价过期，核对零创建、状态持久化及不可再次执行或恢复。验证：Go 1.26 容器设置 `PG_TEST_DSN` 的 `go test -count=1 ./internal/conversion -run TestExecutorRejectsExpiryBeforeChannelCreate -v`、`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 全部通过。限制：未接入真实渠道，创建前校验不保证渠道响应期间报价持续有效；真实终端验收仍待 M2-07b。

验收：同键同输入不新增逻辑 Tracking；不同输入冲突；以真实获批京东账户取得可验证链接与请求证据。模拟测试可先通过，但不得替代真实验收。

### M3：推广订单与看板（P-02 数据、P-10～P-12）

- [ ] M3-01【进行中】新增 / 扩展订单同步、原始数据、状态映射、归因证据；建议 `internal/order` 域，不凭时间金额自动认领。
  - [x] M3-01a【已完成】2026-09-17：新增 `order_raw_events` 迁移和内部 `internal/order` 证据仓储，要求外部密钥注入，以 AES-GCM 加密原始 JSON 并绑定渠道、订单、事件、时间及摘要；同渠道事件号去重，内容或归属变化报冲突，不覆盖原证据。无渠道适配器且未装配运行时写入入口，不产生标准订单、归因或收益。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；覆盖 12 路同事件并发、原文不在密文中、重放冲突、密文篡改、无效输入及密钥版本不符。限制：真实订单回流、渠道签名 / 拉取鉴权、密钥轮换和业务验收待后续任务。
  - [ ] M3-01b【进行中】按渠道状态映射标准订单 / 退款事件，处理乱序回流与历史状态，不从点击推断成交。
    - [x] M3-01b-1【已完成并已提交】2026-09-18：新增标准订单、投影事件及独立退款事件迁移；内部投影只接受已存原始证据，按订单串行处理，乱序旧状态留痕但不回退，重复证据幂等、同证据异映射冲突。部分退款保留主状态并记录金额，全额退款进入 REFUNDED；不创建归因或收益。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过，覆盖跳级状态、迟到事件、重复状态、非法倒退、部分 / 全额退款、12 路并发与原始证据缺失。提交：`feat(M3-01b-1): add order state projection`。限制：渠道字段映射、真实订单回流和资金冲正待 M3-01b-2 / M3-01c 及后续任务。
    - [ ] M3-01b-2【阻塞】根据获批京东 / 淘宝 / 美团订单接口逐渠道建立字段和状态映射，完成真实回流验收。阻塞原因：尚未取得各渠道获批订单接口、字段协议及真实回流测试条件，无法离线证明映射正确；下一步：取得脱敏接口样例、权限和测试订单后逐渠道拆分实现及验收，不以模拟数据冒充真实验收。
  - [ ] M3-01c【进行中】以可信 subId / 渠道位 / 唯一链接凭证精确归因并保留证据；缺证据进入待核验。
    - [x] M3-01c-1【已完成】2026-09-18：新增订单归因迁移和内部存储边界，只接受已落库标准订单及原始证据；可信 subId 精确匹配既有 Tracking，唯一链接凭证只匹配成功转链请求，渠道位仅在 READY 且账户 / 外部位唯一时归因。缺失或未知凭证记录为 PENDING_REVIEW，不按时间 / 金额猜测，不推断消费者、不创建收益；同订单同证据幂等，异证据冲突且不覆盖原记录。验证：Go 1.26.0 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；输入边界单测通过，PostgreSQL 集成用例因未设置 `PG_TEST_DSN` 按约定跳过。限制：真实渠道凭证字段及 READY 位仍依赖 M3-01b-2 / M1-05c-3b 联调，待核验记录的人工处置流程待后续任务。
    - [x] M3-01c-1a【已完成】2026-09-18：恢复隔离 PostgreSQL 16，补跑 M3-01c-1 的数据库集成用例；新增转链请求号非空值唯一索引，避免一份渠道凭证匹配多条转链。补测成功转链请求号精确归因、PENDING 转链保持待核验、重复请求号拒绝。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过。M3-01c-1 原提交的 PG 用例曾跳过，本子任务完成了真实数据库补验；渠道位 READY 路径及真实订单回流仍待授权条件，不宣称业务验收。
- [ ] M3-02【进行中】实现推广订单列表 / 详情、日期渠道推广位筛选、脱敏与所有权校验。
  - [x] M3-02a【已完成】2026-09-18：新增本人订单 `GET /api/v1/promoter/orders` 与 `/{id}`，仅返回 `ATTRIBUTED` 且归属当前已验签用户的订单；按最早渠道订单事件时间、渠道、推广位、订单状态筛选及游标分页。数据库为订单生成随机公开 ID，渠道订单号只显示末四位；详情仅返回已应用状态历史和退款事件数，不外泄原始证据、买家资料或未经确认的收益。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；覆盖待核验排除、跨用户 404、筛选、分页游标脱敏、接口鉴权与参数校验。限制：尚无真实渠道订单、金额 / 收益快照和 H5 页面，M3-02b 未开始。
  - [ ] M3-02b【未开始】在收益快照确认后补收益状态筛选、金额与 H5 页面，完成真实订单和用户端验收。
  - [ ] M3-02c【进行中】uni-app 接入既有本人推广订单只读契约及状态页面；收益快照、真实身份与真实订单验收保持独立，不以推广订单冒充消费者返现订单。
    - [x] M3-02c-1【已完成】2026-09-19：新增 `features/promotion/orders-api.ts` 本人推广订单列表 / 详情 GET 契约、类型及测试，身份提供方必须显式注入；无可用令牌不请求，提供方令牌只放 Bearer 请求头，不持久化、不签发，等待期间令牌改变 / 退出即拒绝旧响应。筛选绑定请求快照，字段强制字符串，校验日期、渠道、推广位、订单状态与结果匹配；日期按整秒加纳秒比较，保留 RFC3339 精度与排他 `to` 边界。校验公开 UUID、脱敏订单号、归因方法、分页、完整历史与退款事件数；输出白名单，不返回额外买家 / 原始证据 / 收益字段，无金额或资金状态推断。40 项契约先实际失败再实现，随后筛选对象变更 / 日期 / 编码三项、非法 falsey 筛选四项、纳秒区间一项实际失败后修正，共 48 项。`npm test` 全量 194 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./internal/order ./internal/httpapi`、`git diff --check` 通过，只读复审及最后筛选 / 精度修正复审无阻断，审查者独立运行 194 项测试 / 类型 / diff 通过；更新 README。未设置 PG_TEST_DSN，数据库集成跳过；仅模拟传输和编译，不接订单页面、身份代理、真实会话或真实渠道订单，M3-02c / M3-02 整体不勾选，后续页面与会话保护由 c-2 验收。本轮逐任务本地提交，不自动推送 / 部署。
    - [ ] M3-02c-2【进行中】本人推广订单独立页面接入、上下文 / 会话 / 筛选失效、详情与分页、加载 / 错误 / 空 / 登录说明及键盘窄屏验收；不签发模拟身份、不把真实会话未接通标为完成。按下列独立子任务验收。
      - [x] M3-02c-2a【已完成】2026-09-19：新增 `orders-model.ts` 复用真实读取契约，显式绑定身份 Ref；退出 / 换会话同步清除订单、筛选、详情及游标并使旧请求失效，新会话重新读取。列表 / 详情独立版本、分页防并发 / 重复 / 循环，失败保留游标；同一会话的 401 即使来自旧视图请求也清空并停止复用当前令牌，旧会话结果不影响新会话。不改变身份层、不持久化或签发令牌。新详情更新摘要，状态 / 地域无关的日期范围等筛选不再匹配时移除旧列表项并保留新详情，失效或更新详情使旧目录页作废；复用已存在纳秒比较函数，仅新增导出，保持排他 `to`。模型所有者可调用幂等 `dispose()` 停止监听、清空缓存及失效请求，Vue 作用域自动销毁，销毁后会话变化 / 读取动作 / 迟到响应均不恢复缓存。15 项状态测试先实际失败再实现，补新详情 / 回补日期三项、迟到 401 两项及审查要求的生命周期三项，分别观察失败后修正，共 23 项。`npm test` 全量 217 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./internal/order ./internal/httpapi`、`git diff --check` 通过；只读审查指出生命周期问题后修正，最终复审无阻断，审查者独立运行 217 项测试 / 类型 / diff 通过。更新 README 与生命周期职责。未设置 PG_TEST_DSN，数据库集成跳过；仅状态 / 模拟传输 / 编译，不是页面、浏览器、真实身份 / 渠道订单 / 微信 / 金额业务验收，M3-02c-2 及父任务不勾选。逐任务本地提交，不自动推送或部署。
      - [x] M3-02c-2b【已完成】2026-09-19：新增独立 `pages/promotion/orders` 和 `PromoterOrders.vue`，推广中心键盘入口 / 返回，消费者订单页及四栏职责不变；接入既有 model / API，默认无身份只显示说明、零请求，真实身份所有者可通过 `promoterSessionKey` 注入 Ref，UI 不签发 / 持久化或提供填写 token 的表单。展示脱敏列表、渠道 / 状态 / 推广位 / UTC 日期筛选（含结束当天，转换次日排他 to）、详情归因 / 已应用历史 / 退款事件数、分页重试及 loading / empty / sanitized error / unavailable / login 状态；不推断收益、到账或退款金额。组件 9 项及页面 2 项先实际 RED 再 GREEN；服务恢复重试补 RED → GREEN，401 显示清理测试通过；uni H5 输入节流会在快速应用读旧值，修正测试工具 CustomEvent 后正确观察 RED，增加 blur 最终值提交后 GREEN。浏览器尺寸断言先 RED（外层 26px / 内层 0px），修复 50px 输入框；清除筛选后迟到节流恢复旧日期亦实际 RED，清除时销毁旧输入实例后同一断言 GREEN；复审补直接 A→B 会话替换，快速输入后新会话草稿被迟到值恢复亦实际 RED，模型筛选重置时也销毁旧输入实例，三屏宽复跑 GREEN。最终 `npm test` 17 文件 231 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./internal/order ./internal/httpapi`、`git diff --check` 通过。Browser plugin not available，已有 Playwright / Chrome 在 320 / 390 / 1280×844 验证实际默认路由与键盘进入零 GET；QA 独立挂载真实页面并注入测试 Ref，真实 uni.request HTTP 由 Playwright 拦截，仅测试端提供身份 / 数据。39 个 GET 的 Bearer 头正确、不在 URL；9 条预期 401 / 503 资源日志，无额外运行错误 / 空白 / overlay / 横向溢出。验证筛选 / 日期校验 / 清除不恢复、键盘分页 / 错误重试、详情历史 / 重试 / 关闭、空态、退出迟到响应、直接换会话草稿不恢复和 401 清理；直接查看 login320 / detail390 截图，文本按钮可读。只读审查发现尺寸及直接换会话边界后修正，最终复审无阻断，审查者独立 231 项测试 / 类型 / diff 通过。更新 README；逐任务本地提交，不自动 push / 部署。PG_TEST_DSN 未设置，数据库集成跳过；无真实身份、生产 API 配置、渠道订单、微信设备或资金验收，父任务保持未完成，真实端到端由 2c 单独记录。
      - [ ] M3-02c-2c【阻塞】接真实身份提供方和部署 API 配置，使用当前本人 / 他人真实推广订单完成登录、退出 / 换身份、跨用户不可见及渠道回流端到端；原因：没有实际手机号 / 微信登录提供方、获批订单测试材料和业务联调条件，开发代理也不转发推广认证接口。依赖身份接入及 M3-01b-2；下一步在真实提供方可用后通过现有 key 绑定实际身份、配置只读服务地址并验收，不能用测试 token / 拦截响应代替真实业务。
- [ ] M3-03【未开始】订单与收益双状态显示，预计个人收益和消费者返现分栏；迟到订单缺规则快照进入待核验。
- [ ] M3-04【进行中】实现看板指标口径、时区、更新时间和分享操作统计；无可信点击时隐藏转化率。
  - [x] M3-04a【已完成】2026-09-18：新增本人 `GET /api/v1/promoter/dashboard`，以 Asia/Shanghai 自然日和同一只读数据库快照统计成功转链、复制上报和有效归因订单；日期上界为次日零点，默认最近 30 天，支持渠道 / 推广位筛选。成功转链按当前 `SUCCEEDED` 请求及更新时间，复制按已存事件号及记录时间，订单按最早订单时间与当前有效状态计数；不返回收益或转化率。验证：Go 1.26.0 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过，覆盖本人隔离、退款排除、筛选、日期边界、接口鉴权和错误脱敏。限制：当前统计基于已有测试 / 空数据，尚无真实订单回流、可信点击、收益快照及 H5 看板，待 M3-04b。
  - [x] M3-04a-1【已完成】2026-09-19：看板 HTTP 返回的 From / ToExclusive / AsOf 转为 UTC，保留原始瞬间、纳秒与 Asia/Shanghai 自然日过滤，不改统计 SQL 或只读快照事务；修复历史 LMT 秒偏移被 Go RFC3339 时区分钟编码截断。Go 原生时区 / JSON 探针确认 1900-01-01 原响应 +08:05 丢失 43 秒，UTC 正确为前日 15:54:17Z；四组真实路由字面量断言现代 / 23h / 25h / 历史秒偏移先 RED，修正后 GREEN。`go test -count=1 ./...`、`go vet ./...`、`go build ./...`、`git diff --check` 通过；只读审查无阻断。更新 API 时间戳约定；PG_TEST_DSN 未设置，数据库集成跳过，未执行迁移 / 生产部署 / 真实渠道对账。此子任务独立本地提交；前端 M3-04b-1 仍未完成，另行验收提交，不自动推送。
  - [ ] M3-04b【进行中】收益快照、可信点击及 H5 看板接入，完成真实业务对账。先按下列子任务接入现有本人只读计数，不把复制上报当点击或已确认收入。
    - [x] M3-04b-1【已完成】2026-09-19：新增 `dashboard-api.ts` 本人看板 GET 契约、DTO / DashboardError / 测试；显式身份 getter，无令牌零请求，Bearer 仅头、uni GET / 10 秒，退出 / 换会话拒绝迟到响应，筛选复制快照。不签发 / 持久化令牌，不请求他人；拒绝未知筛选 / 非字符串 / 无效日历 / 渠道 / 推广位，响应白名单只保留三项非负安全整数计数与 timeZone / from / toExclusive / asOf，不返回额外所有者、原始字段、收益或转化率，复制数不当点击。按真实 Asia/Shanghai Intl 时区验证本地日界及请求范围，默认 30 自然日不绑客户端时钟；实际时长上限遵守后端 366×24 小时，不以固定 UTC+8 / 整数 24h 推断自然日。时区能力缺失失败锁闭 unavailable。首批 52 项空实现下失败后实现通过，补边界 / 一侧筛选 / 错误等用例；非法 transport 结构及审查发现的夏令时四项实际 RED 后修正，增加 LMT 秒偏移 UTC、缺 Intl 及默认范围跨 DST 回归，共 65 项。Go 原生时区探针独立核对 1991 +09 / +08、23h / 25h 及 1900 秒级偏移，相关 HTTP 精度修正已由 M3-04a-1 独立提交。`npm test` 18 文件 296 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./...`、`go vet ./...`、`go build ./...`、`git diff --check` 通过；只读复审无阻断，审查者独立 296 项测试 / 类型 / diff 及 Go wire 用例通过。更新 README。PG_TEST_DSN 未设置，数据库集成跳过；仅契约 / 模拟传输 / 编译，不代表模型、页面、浏览器、真实会话 / 渠道计数 / 资金对账或微信时区运行时验收，父任务不勾选。逐任务本地提交，不自动 push / 部署。
    - [x] M3-04b-2【已完成】2026-09-19：本人看板模型与独立页面、渠道 / 推广位 / 自然日筛选、口径、加载 / 空 / 错误 / 登录、键盘 / 窄屏和旧响应保护已按下列子任务验证；默认真实身份未接入时不请求。此处只标前端本机模拟验收完成，真实身份 / 计数 / 收益业务验收由 M3-04b-3 继续，M3-04b / M3-04 不勾选。
      - [x] M3-04b-2a【已完成】2026-09-19：新增 `dashboard-model.ts` 复用真实看板 GET 契约，显式身份 Ref；初次读取显式刷新，无身份零请求，换会话同步清空计数 / 筛选 / 错误后新身份重读。筛选 / 刷新立即失效旧快照、请求版本防乱序，筛选复制快照且相同筛选不重复读取，显式刷新用于重试；错误 / 未接入没有零数据回退，只有实际三个零计数保留元数据并标 empty，复制大于链接 / 订单不推算漏斗。迟到同会话 401 即使旧筛选响应也清空并禁止复用令牌，旧会话结果不影响新身份；不修改 / 持久化 / 签发身份。非法对象 / 日期 / 未批准字段不能默默查询全部。模型所有者幂等 dispose，Vue 作用域自动停止会话监听、清缓存并使请求失效；销毁后的会话变化 / 刷新 / 筛选 / 迟到结果不恢复数据。17 项状态测试空实现下观察失败再实现通过，补 null / array / false 三项实际 RED 后拒绝，共 20 项。最终 `npm test` 19 文件 316 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./internal/dashboard ./internal/httpapi`、`git diff --check` 通过；只读审查无阻断，审查者独立 316 项测试 / 类型 / diff 通过。更新 README，逐任务本地提交，不自动 push / 部署。PG_TEST_DSN 未设置，数据库集成跳过；仅模型 / 模拟传输 / 编译，不是页面、浏览器、真实身份 / 订单计数 / 渠道或资金验收，2b 与业务验收继续，父任务保持未完成。
      - [x] M3-04b-2b【已完成】2026-09-19：独立本人看板页面、筛选与口径 / 时间显示、错误恢复及键盘窄屏的本机测试验收通过，详见下列子任务；真实身份 / 数据业务验收由 M3-04b-3 继续。
        - [x] M3-04b-2b-1【已完成】2026-09-19：新增本人看板统计组件，渠道 / 推广位 / 上海自然日筛选，三项服务端计数与精确快照，真实零 / 失败恢复、默认未登录零请求、身份切换清理、uni blur / 输入版本隔离及键盘按钮。9 项组件测试观察 RED 后实现 GREEN；补 DST / LMT 秒级偏移与换身份草稿回归，已有筛选挂载用例观察 RED 后以 immediate watcher 修正，共 14 项。`npm test` 20 文件 330 项、`npm run typecheck`、项目 `npm run build:h5` / `npm run build:mp-weixin`、`git diff --check` 通过；独立只读组件审查无阻断。更新 README，逐任务本地提交，不自动 push / 部署。仅实际组件 / 模型 / API 的模拟外部传输测试，组件尚未被生产路由引用，项目构建不代表本组件微信设备验收；路由及浏览器由 2b-2 继续，真实身份 / 数据由 3 继续，父任务未完成。
        - [x] M3-04b-2b-2【已完成】2026-09-19：新增独立 `/pages/promotion/dashboard` 及推广中心键盘入口，返回 switchTab；注入身份默认 null，模型在 setup 自动作用域清理，不改消费者订单 / 京东 API / 开发认证代理。4 项页面测试观察 RED 后接入 GREEN。`npm test` 21 文件 334 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`git diff --check` 通过；独立路由审查无阻断，审查者独立 4 项页面测试 / 类型 / diff 通过。Browser 插件未提供，已有 Playwright / Chrome 在 127.0.0.1:5173 的 320 / 390 / 1280px 验证实际入口 / 返回、身份 / 非空 / 无覆盖溢出、默认未登录零 GET；QA-only 注入身份且真实 uni HTTP 拦截验证计数 / 精确快照、上海包含首尾日期 query、非法日期 / blur、节流输入重置、空 / 错误 / 重试、换身份草稿、退出旧响应及 401 清理。30 次只读 GET、6 条预期 401 / 503 日志、无其他运行错误；直接查看 390px 统计和 320px 未登录截图。薄荷绿 / 白卡视觉沿用已有订单页，三计数无收益图表是有意业务边界。更新 README，逐任务本地提交，不自动 push / 部署。仅本机测试数据及身份，不代表生产提供方 / 真实渠道订单对账 / 微信真机 / 资金验收；M3-04b-3 继续阻塞。
    - [ ] M3-04b-3【阻塞】真实身份、可信点击、收益快照及订单回流接入后验证真实业务对账；依赖 M0 资金决策、身份提供方与获批渠道数据，不能用本机测试计数替代。
- [ ] M3-05【进行中】测试重复 / 乱序订单、无法归因、共享渠道位、匿名买家、部分 / 全额退款、对账回补与指标一致性。先复核已有证据并区分隔离数据库 / 真实渠道 / 资金验收，不凭跳过的测试勾选完成。
  - [x] M3-05a【已完成】2026-09-19：复核原始事件、投影、归因、读取、看板仓储与 9 项数据库用例，新增[异常验收矩阵](./2026-09-19-order-anomaly-acceptance.md)，区分已有分段覆盖、缺失的全链路联测及真实资金验收，拆 M3-05b-1～5。`go test -count=1 -v ./internal/order ./internal/dashboard` 显示 2 项非数据库测试 PASS、9 项因 PG_TEST_DSN 缺失 SKIP，不宣称数据库通过；Docker 同一探针复查及限时 socket 检查无响应，本机 PostgreSQL 二进制未发现。新文档链接 / 方法名与 diff 检查通过，更新专项测试计划。只完成证据复核 / 验收设计，本地逐任务提交，不执行数据库迁移 / 生产部署 / 远端 push；M3-05b / c 和父任务未完成。
  - [ ] M3-05b【阻塞】隔离 PostgreSQL 上联测原始事件 → 投影 → 归因 → 本人订单 / 看板，补共享渠道位、无归因 / 匿名消费者、退款 / 回补与时间边界。PG_TEST_DSN 缺失，本机无 PostgreSQL 二进制，Docker socket 3 秒探针无响应；下一步恢复隔离测试数据库，确认不是保留业务数据的数据库，再执行迁移及真实 SQL / 并发测试，禁止以 SKIP 当 PASS。
  - [ ] M3-05c【阻塞】真实获批渠道订单回流与收益 / 财务对账；依赖渠道授权、身份、M0 资金规则及 M4 收益快照，逐渠道保存脱敏证据，不以本机订单计数替代。

验收：真实测试订单能追溯到推广位、Tracking 和推广者；不能确定消费者时不冒认消费者返现；列表、详情、统计金额可核对。

### M4：收益、结算与统一钱包（P-13～P-16）

- [ ] M4-01【未开始】在 M0 资金决策确认后实现推广收益快照、确认 / 待结算 / 到账状态及奖励唯一键；建议复用统一 commission / wallet 域。
- [ ] M4-02【未开始】新增结算批次 / 明细、收益 / 批次接口及渠道结算证据关联。
- [ ] M4-03【未开始】统一钱包新增来源维度、冲正关联，余额 / 流水 / Outbox 同事务；历史余额迁移前后对账。
- [ ] M4-04【未开始】用户端实现推广资产、批次、流水及复用提现申请 / 记录；明确「批次到账不等于已提现」。
- [ ] M4-05【未开始】实现提现冻结、风险审核、支付幂等、重复回调处理、失败解冻和人工异常队列。
- [ ] M4-06【未开始】测试并发结算 / 提现、重复消息、部分退款、到账后退款、余额不足追偿、支付超时及对账差异关闭。

验收：未结算资金不可提现；同一收益只入账一次；任何余额变化可追溯不可变流水；失败只解冻一次；真实结算和付款能力未验证前禁止开放生产提现。

### M5：活动与后续能力（P-09）

- [ ] M5-01【未开始】实现京东已授权活动目录、活动有效期和指定推广位转链，复用 M2 归因链。
- [ ] M5-02【未开始】验证下架 / 过期 / 无权限活动及优惠变化，完成活动页验收。
- [ ] M5-03【未开始】其他渠道、QR / 海报、团队协作独立立项并补接口与设计图，不默认纳入上述首期交付。

### M6：V3 领券到商品下单（新增，依赖 M0-02 与获批渠道能力）

设计与状态约束见[领券到下单链路](../../22-coupon-to-purchase-design.md)。以下为新研发任务，均未开始；不因 DOC-05a 的领券图稿完成而勾选。

- [ ] M6-01【未开始】逐渠道确认京东、淘宝、美团首发券的领取模式（本站回执 / 平台领取 / 商品加券 / 活动）、适用范围、真实物料、回执 / 订单 / 核销可见性、终端跳转权限及失效机制；形成脱敏能力矩阵。无获批能力的平台只显示未开放，不用模拟券冒充真实活动。
- [ ] M6-02【进行中】建立券 / 商品 / 店铺适用关系、领取模式、活动地域 / 业务、有效期与规则快照，提供券列表、详情、适用商品接口；验证单商品直达、多商品列表、下架、城市限制与范围变更。服务端返回动作文案，客户端不按平台名称猜券类型。
  - [x] M6-02a【已完成】2026-09-17：新增 000010 只读券目录迁移、平台 / 领取模式 / 时效约束和 `GET /api/v1/coupons`，按平台筛选与绑定游标分页，仅返回启用、已核验且未过期的物料；生产空库返回空列表。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...` 通过，覆盖空库、过期 / 停用过滤、跨平台游标及 HTTP 参数校验。限制：尚无渠道导入、详情、适用商品、真实领取及外跳能力；无真实业务验收。
  - [ ] M6-02b【进行中】实现券详情、适用商品 / 店铺与地域业务范围，按渠道证据更新活动状态；完成单商品、多商品、过期、下架与城市限制验收。
    - [x] M6-02b-1【已完成】2026-09-17：新增 000011 商品 / 店铺范围迁移，开放只读券详情和适用商品分页接口，并将城市 / 业务过滤贯穿列表、详情与商品查询；商品券无有效商品、店铺 / 品类券缺范围标识时隐藏。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...` 通过，覆盖多商品分页、过期 / 下架、无映射商品及跨城市不可见。限制：渠道物料同步、报价、领券和真实业务验收未完成；M6-02b-2 仍未开始。
    - [ ] M6-02b-2【进行中】接入获批渠道的物料同步、证据复核与失效更新流程；按渠道实际状态撤销展示，完成真实数据验收。
      - [x] M6-02b-2a【已完成】2026-09-17：新增 000012 脱敏同步事件及仅接受可信渠道核验器输出的事务性券 / 商品发布撤销边界；生产未装配核验器或写入入口。验证：Go 1.26 + 隔离 PostgreSQL 16 测试 12 路同证据并发、重复同步、旧证据拒绝、更新商品映射与撤销后不可见；`go test -count=1 ./...`、`go vet ./...` 通过。限制：暂无真实京东 / 淘宝 / 美团核验器，未取得真实物料或业务验收；M6-02b-2b 未开始。
      - [ ] M6-02b-2b【未开始】取得渠道正式授权后接入京东 / 淘宝 / 美团实际物料核验与失效更新，逐渠道保存脱敏成功证据并验收真实数据。
    - [x] M6-02b-3【已完成】2026-09-19：uni-app 规则详情接入既有只读适用商品分页接口，完成契约与本地交互验证，展示商品身份与有效期，不虚构实时价格、购买链接或领取能力；真实渠道与微信设备另行验收，M6-02b 整体未完成。
      - [x] M6-02b-3a【已完成】2026-09-19：扩展既有客户端 API，新增适用商品 GET 契约及类型，编码券 ID / 地域 / 业务 / 游标，固定每页 20；校验完整 ID / 标题 / 更新与有效期、页长、游标结构、重复与过期商品，区分失效和服务缺失，错误脱敏。PDD / ELEME 不请求，默认 uni 传输 GET / 10 秒 / 无授权头，无价格、购买地址或领取操作。20 项先实际失败再实现；`npm test` 全量 129 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`git diff --check` 通过，只读复审无阻断；更新 README。仅模拟传输和编译，无真实 HTTP 商品服务、渠道、数据库或微信设备验收。响应不含平台字段，3b 必须以已校验券详情绑定平台并保护跨详情旧响应；页面 / 代理及交互尚未完成，父任务不勾选。
      - [x] M6-02b-3b【已完成】2026-09-19：在已校验当前平台的券详情下展示适用商品 ID / 标题 / 更新与有效期，非活动券自动读取只读商品页；加载 / 空 / 错误、刷新和键盘分页重试，禁止重复页及循环游标。关闭、切换券 / 平台 / 地域 / 业务、券到期或商品接口确认券失效时清空商品与游标并忽略旧请求；到期商品隐藏且保留未读分页。确认券失效同时作废先前的目录更多页，防旧结果恢复撤销券。开发代理仅扩展具体 `/products` 路径，领取 / 身份等不代理。12 项状态用例、3 项组件用例先实际失败再实现；另补撤销券被旧目录页恢复回归，观察失败后修正，共 14 状态 / 3 组件用例。`npm test` 全量 146 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`go test -count=1 ./internal/httpapi ./internal/coupon ./internal/app`、`git diff --check` 通过。Browser 插件未提供，现有 Playwright 1.62.1 / Chrome 在 127.0.0.1:5173，经实际 uni 请求与开发代理读取临时本机 HTTP 测试服务：320 / 390 / 1280 屏宽验证页面身份 / 非空 / 无覆盖与溢出、只读商品、分页错误重试、刷新空 / 错误、键盘 / 关闭、到期隐藏、关闭 / 换平台的旧响应隔离和失效券隐藏，51 次 GET 无授权请求、9 条预期 404 / 503 日志、无其他运行错误；直接查看 320 / 390 商品区域截图。视觉沿用 V3 薄荷绿 / 白 / 四栏，真实字段代替图稿示例图片与购买按钮是有意边界。更新 README。未设置 PG_TEST_DSN，数据库集成跳过；仅本地测试数据、非真实渠道 / 现有数据库 / 微信真机 / 领取购买 / 生产部署验收。
        - 复审：只读代码审查及最后撤销 / 旧目录并发修正均无阻断；审查者独立运行 146 项测试、类型及 diff 检查通过。本轮逐任务本地提交，不自动推送或部署。
- [ ] M6-03【进行中】仅对获批且可核实的本站领取实现鉴权领券、幂等领取记录及状态查询；外部领取结果不确定时只查询原请求，不重复领取或假报成功。平台侧领券不得模拟 CLAIMED。
  - [x] M6-03a【已完成】2026-09-17：新增 000013 领取请求持久化与按用户 / 幂等键唯一的存储边界，支持本人读取、同键同输入重放、异输入冲突、结果待查询和可信回执引用终态。验证：Go 1.26 + 隔离 PostgreSQL 16 测试 12 路并发仅一条记录、跨用户不可读、终态不可覆盖及回执约束；`go test -count=1 ./...`、`go vet ./...` 通过。限制：尚无用户可调用接口或获批领取适配器，未证明真实领券成功。
  - [x] M6-03b【已完成】2026-09-17：实现 `IN_SITE_VERIFIED` 券的可注入领取服务、本人状态查询及 HTTP 边界；先保存幂等请求，再以稳定 claimId 调用适配器，超时保持 QUERY_REQUIRED，同键重试只查询原请求。生产未装配获批适配器，因此不注册 POST 路由，GET 仅本人可读。验证：Go 1.26 + 隔离 PostgreSQL 16 验证未知结果恢复、同键冲突、外部券拒绝、跨用户隔离、无回执不置 CLAIMED；全量 `go test -count=1 ./...`、`go vet ./...` 通过。限制：尚无真实渠道领券适配器或业务验收，M6-03c 未开始。
  - [ ] M6-03c【未开始】接入真实获批领取与查询接口，验证领取回执、失效、超时与真实账户状态；未获批平台保持未开放。
- [ ] M6-04【进行中】实现 H5 平台切换与按模式显示「立即领取 / 前往平台领券 / 领券购买 / 去平台领取或购买」、已领取去使用和领取待确认；覆盖登录回跳、已领完、过期、失败、未开通、窄屏及触控。演示券不得开放真实按钮。
  - [x] M6-04a【已完成】2026-09-17：H5 领券页按平台分页读取券目录，展示券额、门槛、适用范围、有效期和详情；读取适用商品，覆盖空列表、未开放、错误、失效与加载更多状态。无获批动作时仅提供「查看规则」按钮；美团只展示不限城市活动。验证：`node --check web/app.js`、Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过。限制：尚无真实渠道券物料、领取、外跳、城市选择及终端业务验收，M6-04b 未开始。
  - [ ] M6-04b【进行中】接入获批领取、外跳、城市选择、登录回跳和状态查询，完成真实终端验收。
    - [x] M6-04b-1【已完成】2026-09-17：券物料新增已核验城市名称并通过只读城市接口提供实际可用城市选项；H5 默认只看不限城市活动，选城后重查首页券、列表、详情和适用商品，切换平台 / 城市清除旧详情与分页游标。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`node --check web/app.js`、`git diff --check` 通过；覆盖过期城市排除、地域筛选、跨城详情不可读及 HTTP 参数校验。限制：当前无获批城市物料，未进行真实终端与渠道业务验收，领取 / 外跳留待 M6-04b-2。
    - [ ] M6-04b-2【未开始】接入获批领取、外跳、登录回跳和状态查询，完成真实终端验收。
- [ ] M6-05【未开始】商品详情重新核对券资格、实时价格与平台，购物用途 Tracking 和获批渠道跳转绑定券 / 商品 / 请求号；与推广员专用转链隔离。
- [ ] M6-06【未开始】以真实测试账户验证领取回执、打开正确商品、渠道下单跳转、测试订单回流与归因；分别记录模拟验证和真实业务证据，未获批前保持关闭。
- [ ] M6-07【进行中】实现平台侧领券 / 活动的独立受控外跳与跳转记录；按渠道允许的 App 唤起、H5 回退和小程序路径处理已安装 / 未安装 / 微信内环境。服务端校验券、地域、终端、有效期和域名；不接受任意客户端 URL，也不把已打开记作已领取。
  - [x] M6-07a【已完成】2026-09-17：新增券外跳准备服务和独立 PostgreSQL 幂等记录；按券有效期、地域 / 业务和领取模式校验，仅接受获批解析器返回的 HTTPS 精确白名单域名，拒绝用户信息、端口、片段、IP 和不可信目标。同键只保留一条逻辑记录，记录渠道证据引用，不把准备或打开当成已领取。生产未配置解析器且不注册路由。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...` 通过；针对同键并发、篡改请求、地域、撤销、未配置解析器及危险地址补充测试通过，`git diff --check` 通过。限制：真实渠道地址、终端承接与 HTTP / H5 流程留待 M6-07b，尚无真实业务验收。
  - [ ] M6-07b【进行中】接入获批渠道地址、App / H5 / 微信承接和端到端测试，开放外跳 HTTP 路由及 H5 操作。
    - [x] M6-07b-1【已完成】2026-09-17：实现已认证用户的外跳 POST 边界、JSON / 幂等键校验、服务端身份绑定与 `READY_TO_OPEN` 响应；未注入受控服务或身份解析器时路由不注册，生产保持关闭。验证：Go 1.26 + 隔离 PostgreSQL 16 全量 `go test -count=1 ./...`、`go vet ./...`、`git diff --check` 通过；HTTP 测试覆盖未装配、未认证、伪造身份 / URL、无效请求、冲突和内部错误脱敏。限制：未接获批渠道目标、H5 按钮或真实终端验收，M6-07b-2 未开始。
    - [ ] M6-07b-2【未开始】接入获批渠道真实地址与终端承接、H5 操作，完成真实测试账户验收。
- [ ] M6-08【未开始】按京东商品加券、淘宝商品加券、美团外卖红包和到店券分别用真实测试账户验收承接页、平台账户券或结算折扣、订单回流与到店核销的可见边界；无回执时只显示待确认，逐渠道保存脱敏证据，不以一平台成功推定其他平台。

验收：领券成功不等于下单成功；页面打开不等于领券成功，到店购买不等于核销；单商品 / 品类 / 店铺券去向正确；同键重试不重复领券或创建逻辑 Tracking；过期、改价、越权、外部结果未知和渠道故障不显示虚假成功。M6-06 仅对有真实回执的路径验收，外部券与终端矩阵另按 M6-08 记录。

### M7：多平台商品 / 活动推广扩展（P-17）

以[多平台推广界面 V3 详细设计](../../23-multiplatform-promotion-ui.md)为范围。首期京东真实单品仍依赖 M2-04c；以下按平台 / 类型分别验收，不以一平台成功推定其他平台成功。

- [ ] M7-01【未开始】建立统一物料目录及平台能力声明、有效期 / 地域 / 终端 / 规则版本模型，前端按真实 readiness 展示，不向未获批渠道开放生成。
- [ ] M7-02【未开始】完成京东商品真实验收后扩展获批京东活动目录、活动预览、按推广位转链与结果核验；与 M5-01 衔接，活动不假装为商品 SKU。
- [ ] M7-03【未开始】核对淘宝媒体 / 推广位及商品 API 授权，实现商品详情 / 转链适配、契约测试和真实账户验收；不得复用京东签名、siteId 或请求格式。
- [ ] M7-04【未开始】独立核对淘宝官方活动会场 ID 与活动转链授权，实现目录 / 有效期 / 转链状态和真实活动验收。
- [ ] M7-05【未开始】核对美团媒体 / 推广位、获批业务、城市和终端，先实现授权活动取链与归因；任意商品直链须另有官方协议及真实证据才立项。
- [ ] M7-06【未开始】实现 H5 推广选品、物料确认、推广位选择、生成状态、分享及本人链接记录；覆盖不同平台切换时清空旧预览、禁用 / 超时 / 过期 / 下架 / 跨用户状态。
- [ ] M7-07【未开始】真实测试订单验证每个平台 / 物料类型的 Tracking、推广位归属、渠道归因与结算边界；分别保存脱敏证据，不能以点击或复制替代成交。

验收：每张商品 / 活动卡拥有独立物料和请求；同键同输入不重复转链，异键 / 异推广位不串链；渠道结果未知不自动重发；未获批的平台显示未开放而非模拟成功。

## 发布验收记录

- [ ] REL-01【进行中】记录设计稿 / 页面 / API / 用例的 P 编号关联，完成窄屏、字体放大、键盘、触控区域与错误状态验证。分本机现有页面与真实身份 / 设备验收，不以本机模拟覆盖未完成业务。
  - [ ] REL-01a【进行中】现有 H5 首页 / 领券 / 推广 / 我的 / 链接准备 / 本人订单 / 看板的 P 编号与入口映射、320 / 390 / 1280px 字体放大和触控 / 键盘检查，发现回归先复现再修正。
    - [x] REL-01a-1【已完成】2026-09-19：修正链接准备文本域在 uni H5 节流输入尚未同步时无法清空 / 迟到输入恢复问题，以版本 key 重建；原生异步替换丢失 aria-label 的实际浏览器回归先失败，onUpdated / nextTick 无效后依实际 DOM 证据改为稳定组件根 MutationObserver（仅 childList / subtree，卸载 disconnect），不观察全局或属性。快速输入未同步的组件回归先 RED 再 GREEN，另补异步原生节点标签回归，共 11 项 LinkInput 测试；`npm test` 21 文件 336 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、`git diff --check` 通过。Browser 插件缺失，现有 Playwright / Chrome 127.0.0.1:5173 的 320 / 390 / 1280px 快速两次 fill → Space 清空 → network idle 仍空、原生标签正确、识别关闭、无溢出 / 运行错误；直接查看 320px 修复截图。独立组件及增量审查无阻断，审查者独立 11 项 / 类型 / diff 通过。更新 README，逐任务本地提交，不自动 push / 部署；非微信真机 / 系统字体 / 全量辅助技术验收，字体发现的问题由 2 继续，REL-01a / REL-01 未完成。
    - [x] REL-01a-2【已完成】2026-09-19：修正首页 390px / 200% CSS 文本下搜索输入挤至 18.984px，header 内容驱动换行、搜索区域 190px 最小布局基准；新增真实 Node / Playwright `test:layout` 回归观察 RED 再 GREEN。截图 / 高度断言又观察 RED：200% native height 18.1875px 裁切 26px 字体，输入改 44px。最终 320 / 390 / 1280px 默认 / 200% 输入宽>=80px、高>=字号、无文档溢出，默认 390px 品牌 / 搜索仍同排，键盘搜索只提示未接通。`npm run test:layout` 1 项、`npm test` 21 文件 336 项、`npm run typecheck`、`npm run build:h5`、`npm run build:mp-weixin`、Markdown 链接及 diff 检查通过；独立审查及高度增量复审无阻断，审查者独立布局测试通过。Browser 插件缺失，现有 Playwright / Chrome 127.0.0.1:5173 本机空券测试数据复验七页面 / 三宽度，CSS 文本放大、可见按钮>=44×44、实际键盘链路、身份 / 非空 / 无覆盖溢出与运行错误；直接查看 390px 修复首屏及完整截图。新增[页面关联矩阵](../../28-client-accessibility-acceptance-20260919.md)、README运行前提和文档索引，不安装依赖、不纳入临时QA脚本 / 截图、不改业务开关。逐任务本地提交，不自动 push / 部署；默认未登录七页不等于已有详情 / 登录态 / 所有文本放大 / 系统字体 / 微信真机或正式无障碍验收，3 与 REL-01b 继续，父任务未完成。
    - [ ] REL-01a-3【未开始】扩展字体放大 / 触控 / 键盘验收到非空券 / 适用商品及 QA-only 提供方身份下的订单列表 / 归因详情 / 看板统计筛选、错误重试等已有实现状态，补充长字段与窄屏证据；默认未登录七页通过不能替代这些状态。仍不开放生产身份或模拟数据。
  - [ ] REL-01b【阻塞】完整真实业务状态、微信真机、系统字体 / 辅助技术和产品视觉验收；依赖身份、渠道及待开发页面，不以 H5 CSS 放大测试替代设备验收。
- [ ] REL-02【未开始】运行 Go 回归、真实 PostgreSQL 并发集成、渠道契约及前端端到端测试，保存命令、版本与结果。
- [ ] REL-03【未开始】演示「首页 → 审核 → 推广位 → 转链 → 订单 → 收益 → 结算 → 提现」及退款反向链路。
- [ ] REL-04【未开始】完成权限 / SSRF / 隐私检查、数据库回滚演练、监控告警与渠道熔断演练。
- [ ] REL-05【未开始】产品验收视觉与交互，渠道负责人验收真实调用，财务验收资金与对账；保存脱敏凭证，不存密钥。

完成状态只在对应证据通过后勾选。文档 / 图稿完成、接口测试完成、真实业务闭环完成分别记录，不相互替代。
