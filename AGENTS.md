# 研发执行约定

## 任务状态与逐任务提交

- 开始研发前，找到对应计划中的任务编号；没有编号先补计划。推广中心任务见 `docs/superpowers/plans/2026-09-16-promotion-center.md`。
- 每项任务显式标注「未开始 / 进行中 / 阻塞 / 已完成」。开始时改为进行中；阻塞时写清原因、依赖和下一步，不勾选完成。
- 只有该任务实现及约定验证通过才标为已完成、勾选 `[x]`，记录完成日期、变更范围、验证命令及结果。图稿完成、模拟验证与真实业务验收分别记录。
- 每完成一个可独立验收的任务，立即创建一个 Git commit，包含该任务实现、测试及计划状态更新，不积攒多个研发任务再统一提交。过大的任务先拆成可独立验收的子任务并编号。
- 提交信息带任务编号，例如 `feat(M1-01): add homepage promotion entry`。以任务编号关联提交，可用 `git log --all --grep="M1-01"` 查询；无需将本次提交哈希写回本次提交以免循环更新。
- 提交前检查 diff、验证结果和暂存文件，只暂存本任务文件或改动块；保留无关用户修改，不提交密钥、真实凭据或敏感数据。
- 提交失败需明确记录「实现已验证 / Git 提交失败」及原因，不宣称已提交。未通过验证不得为了提交而标为完成。
- Git commit 与远端 push 分开处理；用户未要求推送时只做本地提交，不自动部署或修改远端环境。
- 历史任务不能仅凭文件存在补勾全部测试步骤；需要复核证据。每轮汇报已完成任务、验证结果、提交短哈希和剩余阻塞。
